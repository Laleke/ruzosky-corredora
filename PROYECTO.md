# PROYECTO

## Resumen del Proyecto
**RZK Prop** — Plataforma SaaS de administración inmobiliaria para Chile. Centraliza propiedades, contratos de arriendo, propietarios, arrendatarios, pagos, finanzas, tickets de mantención y documentos. Reemplaza procesos manuales (Excel, WhatsApp, correo) por un sistema digital único.

El producto es una **PWA** (no app nativa): funciona como web e instalable en Android e iPhone. Multitenant desde el diseño, pero opera inicialmente con una sola empresa (RZK Prop).

## Estado Actual
**Fase: en PRODUCCIÓN (Vercel), desplegándose con cada push. Transición activa a uso con datos reales (modalidad "Administrador de Contratos de Arriendo" — ver Punto de Continuación).**

**Portal propietario/arrendatario (T20) + Solicitudes de pago + Estado de cuenta (cobranza por WhatsApp) — en prueba manual real por Eduardo desde el celular, en una sesión larga y continua (2026-08-02 a 2026-08-07) con muchas rondas de bugs y features encontrados/pedidos y corregidos/construidos (ver Punto de Continuación).** Admin invita desde la ficha (o por alta express solo con email+teléfono) por **WhatsApp**; cada rol entra a `/portal` — de solo lectura salvo dos excepciones explícitas: el arrendatario puede **"Informar pago"** (queda `pendiente` hasta que el propietario o el admin lo aprueba/rechaza; al aprobar se crea el pago real) y **editar esa solicitud mientras siga pendiente**. Gate de contraseña obligatorio (`profiles.password_set`), autoservicio de **restablecer/cambiar clave** (login + `/portal/cuenta`), página admin `/usuarios` para gestionar accesos, y **notificación push al admin** cuando informan un pago. **40 migraciones (`0001`–`0040`) aplicadas y verificadas en Supabase** (verificado por consulta directa a la BD al cierre de la sesión del 2026-08-07, no solo reportado).

App **RZK Prop** operativa: auth, dashboard con KPIs (Propiedades vs arrendadas, Deuda pendiente, Cargos morosos, Pagos generados del mes) + **Dashboard Operativo (tareas pendientes)**, y módulos **Propietarios, Propiedades (+copropiedad N:M), Arrendatarios, Contratos (+ reajuste de canon vía UF con revisión manual), Cobros (cargos+pagos, listado en acordeón por período, cargos "desfazados" con período de consumo estructurado), Liquidaciones (con descuento automático de gastos), Documentos (Storage privado + versiones), Gastos (fuente oficial), Reportes Financieros y Portal (propietario/arrendatario)**. **23 migraciones históricas (`0001`–`0023`) + 9 nuevas de esta sesión (`0024`–`0032`, portal + solicitudes de pago + cargos desfazados)** aplicadas y verificadas en Supabase. Build de producción verde, `tsc` limpio, **Vitest** con 12 pruebas. **PWA con banner de actualización** ("Hay una versión nueva — Actualizar", vía `SerwistProvider`) para que un deploy nuevo no quede invisible en pestañas ya abiertas.
**UI: patrón de página unificado y consistente en todos los módulos** (listado en tarjetas burdeo, detalle con edición en línea, wizard de una pregunta a la vez con overlay de Cancelar centrado — **los 13 wizards del sistema ya usan el mismo `grid-cols-3` fijo para Cancelar/Atrás/Siguiente**, normalizado 2026-07-31 —, filtro colapsable con buscador `ComboboxOpcion`, fechas siempre en `dd/mm/yyyy` vía `src/lib/fecha.ts`). Fuente de verdad: `docs/DISENO_PAGINAS.md` + `docs/PAGINAS_NUEVAS.md`.
**Filosofía vigente: app centrada en la Propiedad** — el usuario solo elige Propiedad + datos; propietario/contrato/arrendatario se derivan automáticamente. **Roadmap oficial de Hardening**: T5 (gate de rol) y T20 (portal) ya completados esta sesión — quedan T2 (auditoría) y T3 (backups) del Sprint 1. **Backlog QA** (reglas R1–R5) sigue sin avance.
Ciclo operable: propiedad → (propietario/contrato/arrendatario auto) → cargos/cobros → pagos (directos o vía solicitud del arrendatario con aprobación) → gastos → liquidación al propietario (con gastos descontados) → reportes.

## Punto de Continuación (handoff — actualizar al cerrar cada sesión)

**Última sesión: 2026-08-17 — 4 pedidos de Eduardo en un solo mensaje (KPIs del Dashboard + 2 módulos nuevos: Garantía e Incidencias). Detalle completo en `## Últimos Cambios` (entrada 2026-08-17); resumen del estado exacto al cerrar:**

**Estado exacto al cerrar:**
- **Migraciones `0042_contrato_garantias.sql` y `0043_incidencias.sql` ejecutadas y confirmadas por Eduardo** — no hay nada pendiente de correr en Supabase.
- `database.types.ts` **regenerado con el CLI real** (`npx.cmd supabase gen types typescript --project-id rrhhnfbxrxceedgueyss`) tras confirmarse las migraciones — coincidió exactamente con la edición manual hecha antes de correrlas. Bloque de alias de enums (`RolUsuario`, etc., incluidos `EstadoIncidencia`/`TipoMovimientoGarantia`) re-agregado a mano al final, como corresponde tras cada regen (el CLI 2.114.0 no lo emite).
- `tsc --noEmit`, `next build` (46 rutas, incluidas las 3 nuevas de `/incidencias`) y Vitest (28 tests) verdes. **No probado en navegador real** (sin Playwright/chromium-cli en el entorno) — ni el flujo de registrar/eliminar un movimiento de garantía, ni el wizard de incidencia, ni el botón "Generar gasto".
- Commits **sin crear todavía** — no se pidió commitear en esta sesión, el trabajo queda en el working tree.

**Qué se construyó (ver detalle línea por línea en `## Últimos Cambios`):** KPI "Pagos recibidos" excluye pago directo a servicio; tarjeta "Deuda pendiente"→"Deuda vencida"; módulo Garantía embebido en Contrato (tabla `contrato_garantias`, sin columna de saldo); módulo Incidencias de nivel superior (tabla `incidencias`, con proveedor/seguimiento/vínculo opcional a Gasto).

**Decisiones de alcance confirmadas con Eduardo antes de implementar** (vía `AskUserQuestion`, no asumidas): Garantía se lleva como **historial de movimientos** (no un monto único), e Incidencias con **proveedor y seguimiento completo** (no la versión más simple sin costo/proveedor).

---

**Sesión anterior: 2026-08-16 (corta, un solo entregable) — campo "nombre" opcional en Cargos, a partir de una captura de pantalla de Eduardo señalando la zona de la dirección en el detalle de un cobro.**

**Estado exacto al cerrar:**
- Migración `0041_cargos_nombre.sql` (`alter table cargos add column nombre text`) **ejecutada por Eduardo y confirmada** — no hay nada pendiente de correr en Supabase.
- Commit `2527292` (`feat(cobros): agrega nombre opcional al cargo, editable en línea`) creado sobre `a2be857`, **en `main` local pero AÚN NO PUSHEADO** (rama 1 commit adelante de `origin/main`) — Eduardo no pidió push explícito. **Confirmar con Eduardo y pushear al retomar**, o hacerlo si él lo pide en el próximo mensaje.
- `tsc --noEmit` y `next build` verdes. **No probado en navegador real** (sin Playwright/chromium-cli en el entorno) — el pedido original vino de una captura de pantalla mostrando el detalle de un cobro (`Luz · [dirección]`) con la zona de la dirección resaltada en amarillo, pidiendo usarla para el nombre del cobro.

**Qué se construyó:**
1. **Cargos: campo `nombre` opcional** (ej. "Luz depto 907-A") para identificar un cobro más allá de "Tipo · Período". `actualizarCargo`/`crearCargo` (`features/cobros/actions.ts`) guardan `texto(formData, "nombre")`; edición en línea en `detalle-cargo.tsx` con el mismo patrón toggle "Editar" ya usado en el resto del sistema (input "Nombre del cobro (opcional)" al inicio del formulario, arriba de Tipo/Período — decisión de diseño: como el nombre no está dentro del `<form>` en el header sino que se renderiza ahí para respetar el layout visual pedido, se sacrificó la posición exacta del input editable vs. el texto en modo lectura, ver detalle abajo). En modo lectura se muestra como línea propia bajo el título ("Luz · agosto 2026" + nombre debajo), junto con la dirección de la propiedad (que sigue siendo fija, no editable — se deriva del contrato, nunca cambia con el cargo). Tarjeta de listado (`/cobros`) también muestra el nombre si existe, como sufijo del tipo de cargo (`Tipo · nombre`) — agregado por consistencia dentro del mismo módulo, no pedido explícitamente pero mismo dato.
2. **Decisión de alcance explícita, confirmada con el usuario antes de implementar** (vía pregunta directa, no asumida): el nombre se muestra **junto** a la dirección en esa zona (ninguno reemplaza al otro) — se preguntó porque había 3 formas razonables de resolverlo (reemplazar dirección, mostrar ambos, o hacerlo obligatorio) y la elegida fue "todo, dirección y nombre en esa zona".
3. **El wizard de creación (`cargo-wizard.tsx`) NO se tocó** — el nombre solo se puede cargar después, vía "Editar", no en la creación. Esto fue deliberado (no solicitado, evitar alcance extra) — **si en el futuro se pide poder nombrar un cargo desde el momento de crearlo**, agregar un paso opcional al final del wizard (después de "observaciones"), mismo patrón que el resto de los campos opcionales ahí.
4. **Portal del arrendatario (`features/portal/cargo-detalle.tsx`) NO se tocó** — ese detalle también muestra tipo+período+dirección pero es de solo lectura y no fue parte del pedido (la captura era de la vista admin, con botones "Editar"/"Eliminar cargo" que el portal no tiene). Si se quiere mostrar el nombre ahí también por consistencia, es un cambio de una línea (`{cargo.nombre && <p>...}</p>` antes de la dirección, línea ~209).

**Última sesión previa: 2026-08-15 — ronda de mejoras puntuales en Cobros/Pagos + UI transversal (combos), y una investigación larga y sin resolver del "Dashboard se queda pegado" que terminó revertida. Todo commiteado y pusheado a `main`, en commits chicos por entregable (autorizados uno por uno por Eduardo). Sin migraciones nuevas esa sesión — no había nada pendiente de ejecutar en Supabase.**

**Resumen por tema (orden cronológico):**

1. **Editar cargo y editar pago** (antes solo se podían crear): `actualizarCargo`/`editarPago` en `features/cobros/actions.ts`; edición en línea en `detalle-cargo.tsx` (mismo patrón que Propietarios/Contratos — toggle "Editar" en el mismo grid, no wizard ni modal); wizard de pago (`pago-wizard.tsx`) reutilizado con prop `pagoExistente` para editar. Listado de pagos de un cargo pasa de `<table>` a tarjeta estándar (`pagos-lista.tsx`, con "ver más información" e íconos editar/eliminar).
2. **Wizard "Informar pago" del portal guarda borrador en `localStorage`** (solo al crear una solicitud nueva, no al editar una existente) — antes el arrendatario perdía todo el avance si salía a WhatsApp a buscar el comprobante y volvía.
3. **Bug real corregido — combos de toda la app que no reabrían bien**: `Combobox`/`ComboboxOpcion`/`SelectCombo` (`src/components/`, usados en ~35 selectores: Propiedad, Región/Comuna, Banco, Tipo de cuenta, Estado, Tipo de cargo, Medio de pago, etc., en filtros/wizards/detalle de todos los módulos) reabrían un valor ya elegido mostrando solo esa opción — el texto del campo (el label ya seleccionado) se usaba también como filtro contra sí mismo. Corregido con un flag "recién abierto" que muestra todas las opciones hasta que el usuario escribe algo nuevo. También dependían solo de `onBlur` para cerrarse (poco confiable en PWA/navegadores móviles): se agregó un listener de clic/touch fuera del componente, más alternar abrir/cerrar al re-tocar el input ya enfocado. **Como el bug estaba en los 3 componentes compartidos, corregirlo ahí lo arregló en todas las pantallas de una sola vez** — no hizo falta tocar cada wizard/filtro por separado.
4. **"Ver más información" → "Ocultar información"** al expandir, en las 11 pantallas que usan ese patrón de disclosure (Cobros, Pagos, Contratos, Propietarios, Arrendatarios, Propiedades, Documentos, Liquidaciones, Gastos y sus equivalentes en el portal) — con Tailwind `group-open:`, sin JavaScript.
5. Logo "RZK Prop" del sidebar (admin y portal) ahora es link a inicio (`/dashboard` / `/portal`), en desktop y en la topbar móvil.
6. Filtro "Estado" de Cobros pasa de `SelectStyled` (select nativo) a `ComboboxOpcion`, igual que el resto de los filtros de esa pantalla.
7. **Cobros**: separador entre cargos dentro de un período engrosado (`divide-y-2 divide-white/20`) para distinguirlo del separador más tenue interno de "ver más información"; cargos con saldo $0 se ven desvanecidos (`opacity-50`); el resumen del período pasa de "Deuda pendiente" a **"Deuda vencida: $monto"** (solo lo realmente vencido, no toda la deuda pendiente) o, si no hay nada vencido, **"Deuda pagada: $monto"** en verde (monto total pagado del período).
8. **N+1 real corregido en `listPendientesLiquidar`** (`features/liquidaciones/queries.ts`, usada por el Dashboard y por `/liquidaciones`): recorría los propietarios activos uno por uno con `await` secuencial (~6 consultas por propietario dentro de `calcularLiquidacion`, que asumía "a esta escala es aceptable"). Corregido a `Promise.all`. **Se mantiene** — es una mejora real e independiente del punto 9.
9. **INVESTIGACIÓN LARGA SIN RESOLVER, REVERTIDA — "el Dashboard se queda pegado"**: Eduardo reportó el Dashboard visiblemente colgado en el celular. Se probaron y descartaron, EN ORDEN, con evidencia real (no solo suposición):
   - Lentitud del backend → descartada con `vercel logs` reales: cada carga completa del Dashboard (todas sus consultas) tarda 0.5–3.5s, normal, sin errores.
   - Volumen de consultas → descartada: con la página reducida a 1 sola tarjeta (menos de 3 consultas) el problema seguía igual.
   - Señal de red débil → descartada: pasaba igual en WiFi que en datos móviles.
   - Reintentos manuales del usuario (tocar la pantalla, reabrir la app) → descartado, Eduardo confirmó que solo esperaba sin tocar nada.
   - Ahorro de batería/restricciones de Android → descartado, Eduardo confirmó que esa configuración siempre estuvo igual.
   - **Hallazgo real con `vercel logs --json`**: en cada prueba, Vercel registraba entre **7 y 12 invocaciones completas e independientes de `/dashboard` en menos de 15-30 segundos** — no una request lenta, sino muchas requests cortas repitiéndose solas a un ritmo fijo de ~2-3s, sin relación con cuántas consultas disparaba la página. **No se encontró qué dispara esas repeticiones** (se sospechó del service worker — `navigationPreload`/`NetworkFirst` — o de Chrome/Android, sin confirmar; se descartó código propio: no hay ningún `setInterval` ni refresco automático en toda la app).
   - Se probaron dos intentos de fix (agregar `loading.tsx` a `/dashboard` y `/portal` para esqueleto instantáneo; agregar `networkTimeoutSeconds: 8` a las reglas `NetworkFirst` de navegación en `sw.ts`) — **ninguno resolvió el síntoma**, y Eduardo confirmó que **antes de agregar `loading.tsx` la navegación al Dashboard no se sentía lenta en absoluto**. Conclusión: sin `loading.tsx`, Next.js mantiene la pantalla anterior visible durante la navegación (no hay un estado "nuevo" que mostrar), así que cualquier repetición de fondo que ya existiera era invisible; al agregar `loading.tsx` esa condición preexistente se hizo visible como pantalla "pegada". **Todo revertido** (`page.tsx`, `queries.ts` del dashboard y `sw.ts` vueltos a su estado previo; ambos `loading.tsx` eliminados) para devolver el comportamiento exacto de antes de esta sesión. Se conservó únicamente el fix real del punto 8 (N+1), que es independiente.
   - **Pendiente real si se retoma**: investigar por qué Vercel recibe múltiples invocaciones repetidas de una sola navegación al Dashboard — candidatos sin confirmar: el service worker (Serwist/Workbox `navigationPreload`), o comportamiento propio de Chrome/WebView en el dispositivo de Eduardo. Cualquier intento de arreglo (incluyendo reintroducir `loading.tsx`) debería empezar por instrumentar de nuevo (`console.log` con timestamps, ver commit `e71ea47` como referencia de cómo se hizo) y correlacionar con `vercel logs --json` en tiempo real mientras Eduardo reproduce, en vez de asumir causa antes de tener el dato.
10. **Dashboard — "Tareas pendientes" reformateado**: de lista de filas (label + link + contador) a tarjetas compactas iguales a los KPI existentes (ícono + número + etiqueta), reutilizando el mismo componente `Kpi`. La sección se oculta completa si no hay tareas pendientes.
11. **`~/.claude/pwa-update-banner.md`** (fuera de este repo, playbook personal transversal) actualizado con las medidas reales del banner/botón de RZK Prop (posición, ancho, padding, radios, tamaño de ícono — sin colores), para reusar en el próximo proyecto con PWA que lo necesite.

`tsc --noEmit`, `next build` y Vitest (28 tests) verdes en cada entrega de esta sesión. **Ningún cambio se verificó en navegador real por mí** (sin Playwright/chromium-cli en el entorno) — todo el ajuste vino de reportes y capturas de Eduardo probando en producción real, y de logs reales de Vercel (`vercel logs`, CLI ya autenticada en este equipo) consultados directamente para la investigación del punto 9.

**Lección de esta sesión a tener presente siempre**: antes de agregar un `loading.tsx` (o cualquier estado de carga nuevo) a una ruta que antes no lo tenía, considerar que puede **revelar** un problema de fondo preexistente en vez de causarlo — no asumir que el estado de carga nuevo es el origen del síntoma solo porque apareció al mismo tiempo. Y, más en general: cuando una hipótesis de causa raíz no se puede confirmar con evidencia real (logs, no solo lógica), preferir revertir a un estado conocido-bueno antes que seguir apilando intentos de fix sobre una causa no confirmada.

### Resumen sesión previa: 2026-08-02 al 2026-08-07 — sesión larga y continua probando en producción real (celular + capturas), con muchas rondas de fixes y features. Todo commiteado y pusheado a `main`. Las 7 migraciones nuevas de esta sesión (`0034`–`0040`) están CONFIRMADAS aplicadas en Supabase (verificado por consulta directa a la BD al cerrar, no solo "Eduardo dijo que sí") — no hay ninguna migración pendiente de ejecutar al momento de escribir esto.

**Estado exacto de las variables de entorno en Vercel**: `NEXT_PUBLIC_VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `VAPID_SUBJECT` ya están cargadas por Eduardo y el sitio respondió bien tras el redeploy. **No verificado todavía**: que una notificación push real llegue a un dispositivo (nadie probó el flujo completo Activar → informar un pago → recibe la notificación). Es lo primero a confirmar en la próxima sesión si Eduardo reporta que no le llegan avisos.

**Resumen por tema de todo lo construido en esta sesión (orden cronológico):**

1. **Ícono "ver contraseña" invisible**: `text-white/60` sobre un input con `bg-white` — blanco sobre blanco. Corregido a `text-muted`/`hover:text-ink` en login y en el formulario de contraseña del portal. (Esto explicaba por qué un pedido de una sesión anterior "parecía no estar implementado" — sí lo estaba, solo que no se veía.)
2. **Ver comprobante forzaba descarga automática** (`createSignedUrl` con opción `download`) en vez de mostrarlo — corregido en comprobante de pago y comprobante de solicitud.
3. **Logo "RZK" + "RZK Prop" redundante** → "RZK" + "Prop" en ambos sidebars (admin y portal), desktop y móvil. Repetido más adelante en el membrete del estado de cuenta (mismo bug, mismo fix: `nombreJuntoAlBadge()`).
4. **Cargos: "Vencido" derivado + colores**: "Parcial"/"Pendiente" a gris; "Vencido" (rojo) se deriva de `saldo_pendiente > 0 && fecha_vencimiento < hoy` — antes solo se calculaba en el admin, ahora también en el portal del arrendatario.
5. **Detalle editable de "pago informado"** (`/portal/cargos/[id]`): reemplaza el botón "Editar" directo al wizard por un ícono Ojo → vista con tarjetas de solo lectura (cargo, arrendatario, propietario) + sección "Pago informado" editable in-place, patrón `detalle-propiedad.tsx`.
6. **INCIDENTE — caída total del sistema por recursión RLS (migración `0035`, corrige `0034`)**: mismo patrón exacto que ya pasó en `0028` — una policy nueva sobre `propietarios_propiedades` consultaba `contratos`, que ya tenía una policy que consulta de vuelta a `propietarios_propiedades`. Postgres detecta el ciclo y rompe la tabla para TODOS los roles, no solo el que lo disparó. Corregido con funciones `SECURITY DEFINER`. **Ver `## Riesgos Activos` para la regla dura antes de escribir cualquier policy RLS nueva que cruce tablas.**
7. **Combos al estándar de la app**: varios selects nativos (tipo de cargo, categoría/estado de gasto, selector de contrato en ambos wizards) seguían con `SelectStyled` en vez de `ComboboxOpcion` (buscador con dropdown propio, no el nativo del sistema operativo).
8. **Tarjeta de cobros (admin)**: texto chico = dirección completa de la propiedad (con número); texto grande = concepto (Arriendo/Gasto común/etc) — antes al revés, con el N° de contrato (casi siempre vacío) arriba.
9. **Performance de `/cobros`**: el acotado por defecto (agregado para que "volver de Solicitudes" no fuera lento) escondía cargos YA PAGADOS de meses anteriores — un pago recién registrado desaparecía de la vista del admin. Corregido: ventana de últimos 12 meses + cualquier cargo con saldo pendiente sin importar antigüedad.
10. **Tipo de cargo obligatorio, sin valor por defecto**: el wizard preseleccionaba "Gasto común", así que se podían crear cargos con ese tipo sin que nadie lo eligiera. Además el servidor convertía en silencio cualquier tipo vacío/inválido en "otro" — la misma vía por la que cargos de luz/agua/internet quedaron mal etiquetados antes (ver deuda técnica abajo). Ahora el servidor rechaza en vez de corregir por su cuenta.
11. **Módulo completo "Estado de cuenta"** (migraciones `0036`, `0037`, `0038`): informe de cobranza por WhatsApp (PDF o link público con token) que muestra SOLO cargos vencidos (no los por vencer, no los pagados). Incluye destino de pago configurable por contrato (transferencia a la corredora/propietario) y por cargo (pago directo a servicios como luz/agua, que se informan aparte y NO suman al total a transferir). Rediseño del documento siguiendo una plantilla de referencia que Eduardo mandó.
12. **Portal — cargos agrupados por período** (colapsable, abierto solo si hay deuda), orden estado→vencimiento→ítem (arriendo primero), pagados resumidos en una línea plegable, vencimiento en negrita.
13. **Contratos — término y número mostrables**: `terminoMostrar()` (con tests) muestra "Indefinido" o "Indefinido (renovado desde...)" en vez de una fecha vieja que sugiere que el contrato ya no rige; `numeroContratoMostrar()` cae a un id corto porque `numero_contrato` viene nulo en los contratos reales.
14. **Botón atrás del celular ya no cierra la PWA** desde las pantallas de inicio (`GuardiaAtras`, entrada centinela en el historial). Botón de menú móvil movido a la izquierda del logo.
15. **Comprobantes de pago sin propiedad ni nombre distinguible (migración `0039`)**: se creaban con `propiedad_id` null (por eso desaparecían del listado de Documentos al filtrar) y todos con el mismo nombre. `registrarDocumento` ahora deriva la propiedad desde el contrato en la raíz; los dos flujos que crean comprobantes guardan propiedad, arrendatario, fecha y nombre con concepto+período.
16. **Notificación push al admin (migración `0040`)** cuando un arrendatario informa un pago — cierra la deuda técnica de "sin notificación proactiva". Banner que invita a activar sin tener que ir a Configuración, reaparece cada sesión nueva mientras no se active.
17. **`vitest.config.ts` nuevo**: Vitest no lee los `paths` del tsconfig; sin el alias `@/` declarado ahí, ningún módulo que lo use se puede testear. Suite pasó de 12/13 a 28 tests.

**Verificado con datos reales, NO requiere código**: los comprobantes de solicitudes aprobadas ya funcionan; lo que faltaba en Documentos eran los **contratos en PDF** — nadie los ha subido (decisión de Eduardo: los sube él manualmente, la app no los genera).

`tsc --noEmit`, `next build` y Vitest (28 tests) verdes en cada entrega de esta sesión. Como en sesiones anteriores, **ningún cambio se verificó en navegador real por mí** (sin Playwright/chromium-cli en el entorno) — cada ajuste vino de datos consultados directamente en Supabase (con la service role key de `.env.local`, siempre solo lectura salvo las migraciones explícitas) y de capturas/reportes en texto de Eduardo probando en producción real.

### Resumen sesión previa: 2026-08-02 — Eduardo probando el Portal + Solicitudes de pago en producción real desde el celular; 4 rondas de bugs/pedidos encontrados y corregidos. Todo commiteado y pusheado. Commits en orden: `94cc7e0` → `5ff11ed` → `520f4cd` → `3f71454`.

**Resumen por tema:**

1. **Bug real — cargos de luz/agua/internet se guardaban como "Otro"**: `TIPOS` (lista de validación server-side en `src/features/cobros/actions.ts`) no incluía esos 3 valores — cualquier cargo de esos tipos ya creado en producción quedó mal etiquetado en la BD y hay que corregirlo a mano (el código ya no lo va a repetir, pero no arregla lo histórico).
2. **Bug real — submit implícito de los wizards de pago, encontrado en 2 rondas**:
   - Primera ronda: Enter/tecla "Ir" del teclado móvil en el paso "Referencia" (penúltimo, antes de agregar el paso de comprobante) disparaba un submit nativo del navegador antes de llegar al paso de adjuntar — se interceptó con `onKeyDown`, insuficiente.
   - Segunda ronda (**caso real detectado por Eduardo**: solicitud de Paul Onetto $132.614, comprobante subido en el navegador pero nunca llegó a guardarse en la fila): el intercepto de Enter no es confiable — algunos teclados móviles disparan el submit sin pasar por un evento de teclado que JS pueda interceptar a tiempo. **Fix definitivo (commit `3f71454`)**: el `<form>` exige una marca explícita (`permitirSubmit`, puesta solo por el botón real de guardar o "Sí, guardar" al salir) antes de dejar pasar cualquier submit — bloquea el envío espontáneo en el punto de origen sin depender de adivinar qué lo dispara. **Esa solicitud específica de Paul Onetto quedó sin comprobante guardado y hay que rehacerla** (editarla ahora que el fix está desplegado, o volver a informarla).
   - También se cambió a que el comprobante de la solicitud del arrendatario se suba de inmediato al elegirlo (server action con cliente admin) en vez de viajar como `File` crudo hasta el submit final — mismo patrón que ya usaba el wizard de pago del admin.
3. **Edición de solicitud de pago pendiente**: mientras una solicitud siga `pendiente` (no aprobada/rechazada), el mismo arrendatario que la creó puede editarla desde `/portal/cargos` ("Editar pago informado") — antes esa acción quedaba oculta sin alternativa. Nueva policy RLS de UPDATE (migración `0033`, **confirmada aplicada** por Eduardo) + server action `editarSolicitudPago`.
4. **Autenticación — restablecer/cambiar clave**: botón ver/ocultar contraseña en login y en el formulario de contraseña del portal; nueva sección "Mi cuenta" (`/portal/cuenta`) para cambiar la clave ya logueado; flujo de "¿Olvidaste tu contraseña?" desde login (`/recuperar-clave` → `/actualizar-clave`, vía recovery estándar de Supabase Auth). **Depende de que el envío de correo de Supabase Auth esté configurado** (SMTP propio o el servicio por defecto) — a diferencia del resto del sistema, que evita el email a propósito y usa WhatsApp; sin verificar con Eduardo si el SMTP está configurado. `/invitacion` ahora salta directo a `/portal`/`/dashboard` si el navegador ya tiene sesión activa (evita mostrar "Registrarme" a alguien ya logueado).
5. **UI — tarjetas de Contratos**: la línea superior mostraba `numero_contrato` (a menudo "—" vacío) — ahora muestra el tipo de propiedad; la línea principal usa la etiqueta completa (dirección + número + depto, vía `etiquetaPropiedad()`) en vez de solo la dirección cruda.
6. **`etiquetaPropiedad()` (`src/lib/propiedad.ts`) ya no antepone el código interno** (ej. "PRD0001") — se veía como un ID técnico en todos los selectores de propiedad del sistema. El código queda solo como respaldo si la propiedad no tiene dirección cargada.
7. **Dashboard**: tarjeta "Pagos generados" → "Pagos recibidos" (dato correcto, etiqueta confusa) y ahora se mide por el **período del cargo**, no por `fecha_pago` (el arriendo se paga por adelantado, así que filtrar por fecha de pago dejaba afuera pagos hechos antes de que empezara el mes que cubren). Nueva tarea "Solicitudes de pago por aprobar".
8. **~40 selects nativos en 27 archivos** (filtros, wizards, fichas de detalle) pasan a `SelectStyled` para tener el mismo look (`ui.input` + flecha) que el resto de los selectores — antes convivían dos formatos distintos sin razón funcional.
9. **`autoFocus` en pasos de wizard**: se probó sacarlo (hipótesis: el teclado abriéndose/cerrándose en cada paso se sentía como lentitud) pero Eduardo aclaró que la lentitud percibida era **al cambiar de página**, no entre preguntas de un wizard — se revirtió el cambio, `autoFocus` se mantiene en los wizards (solo sigue sacado de `ComboboxOpcion`, como ya estaba antes de esta sesión).
10. **Pendiente sin resolver — posible bloqueo de capturas de pantalla**: Eduardo reportó no poder tomar captura de pantalla en RZK Prop (sale en negro/bloqueada). Una PWA no tiene forma de activar `FLAG_SECURE` de Android (eso es exclusivo de apps nativas) — se le pidió confirmar si el bloqueo es específico de la app o general del navegador/perfil (Carpeta segura, perfil de trabajo/MDM). **Sin respuesta aún** — no se tocó código porque no hay nada en el codebase que pueda causar esto.

`tsc --noEmit`, `next build` y Vitest (13 pruebas) verdes en cada entrega. **Ninguno de estos cambios se verificó en navegador real por mí** (sin Playwright/chromium-cli en el entorno) — todo el ajuste vino de una foto y de reportes en texto que Eduardo mandó probando en producción real desde el celular, incluyendo el caso real de la solicitud de Paul Onetto que expuso el bug de submit implícito.

### Resumen sesión previa: 2026-07-30/31 — Portal propietario/arrendatario (T20) completo, Solicitudes de pago, y una serie de rondas de ajuste tras probar en producción.

Commits de esta sesión, en orden: `689d694` (T20 base) → `1b2854f` (fix link WhatsApp + comprobante en pagos + KPI + alta express) → `3d49671` (fix link consumido por preview de WhatsApp + botón único + ícono real) → `58571eb` (solicitudes de pago + paridad de info en portal + ajustes Dashboard) → `275ab91` (**incidente**: recursión RLS tumbó la app entera, corregido + dirección vacía en portal + copy "Informar pago") → `091a80b` (monto sin bloqueo con marca de revisión + gate de contraseña obligatorio + `/usuarios` + wizards normalizados + regla día 21 + cargos desfazados).

**Resumen por tema (no por commit) de todo lo construido esta sesión:**

1. **T20 — Portal propietario/arrendatario**: alta por ficha existente o alta express (solo email+teléfono, la persona completa su propia ficha en `/portal/completar-perfil`); invitación **por WhatsApp** (nunca email); gate de rol (admin↔portal) en ambos layouts; árbol `/portal/...` de solo lectura (contratos, cargos, propiedades, liquidaciones, documentos según rol).
2. **Bug del link de invitación — dos causas encontradas y corregidas**: (a) se usaba `action_link` de Supabase en vez de `hashed_token` (formato que nuestra ruta no podía leer); (b) **WhatsApp precarga el link compartido para la vista previa**, consumiendo el token de un solo uso antes de que la persona lo tocara — se agregó `/invitacion`, página pública intermedia con botón "Registrarme" que recién ahí dispara el consumo real. **Lección aplicable a cualquier link de un solo uso compartido por un canal con preview automático.**
3. **Gate de contraseña obligatorio** (`profiles.password_set`): corrige que alguien completara el registro y quedara sin contraseña utilizable desde otro dispositivo. Página `/usuarios` (admin) para reenviar/restablecer sin ir ficha por ficha.
4. **Solicitudes de pago**: el arrendatario "Informa" un pago (mismo wizard que el admin) que queda `pendiente` hasta que el propietario o el admin lo aprueba (crea el pago real) o rechaza (con motivo). Monto **no se bloquea** si supera el saldo — se marca para revisión. Única tabla con INSERT no-admin en todo el esquema.
5. **Incidente de producción**: una policy RLS nueva (`0027`) generó recursión infinita entre `contratos` y `contratos_arrendatarios`, tumbando la app completa (todos los roles) — corregido con una función `security definer`. **Regla dura documentada en `## Riesgos Activos`: nunca una policy RLS nueva puede consultar una tabla cuyas propias policies consultan de vuelta a la tabla original.**
6. **Cargos "desfazados"** (gasto_comun/luz/agua/internet): período de consumo real guardado como fechas estructuradas, distinto del `periodo` de cobro/vencimiento (que no cambia). Aviso en Dashboard/Cobros cuando un contrato termina y falta generar el último cargo desfazado (retención de garantía).
7. **Regla del día 21**: la generación asistida y el aviso de "arriendo sin generar" solo se calculan desde el día 21 de cada mes.
8. **13 wizards normalizados** al mismo patrón de botones (`grid-cols-3` fijo).

`tsc --noEmit` y `next build` verdes en cada entrega. **Ninguno de estos cambios se verificó en navegador real por mí** (sin Playwright/chromium-cli en el entorno) — todo el ajuste vino de capturas y pruebas reales que Eduardo hizo en su celular, incluyendo el incidente de la app caída (detectado por Eduardo, diagnosticado con el log real de Vercel que él copió).

### Resumen sesión previa: 2026-07-29 (segunda mitad — 4 commits más sobre la misma fecha)

App **en producción** (Vercel), redeploy con cada push. Repo `github.com/Laleke/ruzosky-corredora`. Rama `main` **pusheada hasta `dd2eec8`**, autorizado explícitamente por Eduardo en cada push. Sin cambios sin commitear al cerrar. `tsc --noEmit` y `next build` verdes tras cada entregable — **ninguno de estos cambios se verificó en navegador real** (sin Playwright/chromium-cli en el entorno); todo el feedback de ajuste vino de capturas de pantalla que Eduardo mandó desde su celular.

### ✅ Todas las migraciones ejecutadas (`0001`–`0023`), Eduardo confirmó

`canon_actual`, `canon_uf_base`, `fecha_proximo_reajuste` (contratos) y la política RLS de DELETE de Contratos (`0020_contratos_delete.sql`) ya existen/aplican en producción. "Eliminar" en Contratos debería funcionar correctamente ahora.

### Resumen de los 4 commits de esta sesión (`f6629b0`→`6409c8b`→`8c8be69`→`dd2eec8`)

1. **`f6629b0`** — Los avisos y paneles de resumen de Cobros/Liquidaciones (contratos sin arriendo, reajuste pendiente, generación asistida, deuda total, liquidaciones pendientes) pasan de tarjeta blanca a burdeo/texto blanco. Reportes completo a burdeo también: KPIs, paneles de gráficos **y la paleta de colores de `src/components/charts.tsx` recalibrada** (grillas/ejes/barras en tonos claros — la paleta original era oscura, pensada para fondo blanco, invisible sobre burdeo). **Bug real corregido**: `aplicarReajusteUF` avanzaba `fecha_proximo_reajuste` un período completo aunque se aplicara el reajuste *antes* de la fecha de revisión (corrigiendo el canon a mitad de trimestre) — se habría saltado la próxima revisión real; ahora solo avanza la fecha si hoy ya alcanzó o pasó `fecha_proximo_reajuste`.
2. **`6409c8b`** — Tres correcciones de formato + una regla de negocio nueva:
   - **Overlay de "Cancelar" unificado** en los 8 wizards (centrado, fondo oscurecido `bg-black/60`, igual que Propietarios) — se encontró que `propiedad-wizard.tsx` (el wizard de referencia original) había quedado con el estilo viejo sin el overlay fijo; corregido junto con los 3 wizards nuevos y el de Liquidaciones (que no tenía botón Cancelar).
   - `ComboboxOpcion` ya no hace `autoFocus` en los pasos de wizard (abría la lista desplegada solo al entrar al paso, se veía mal).
   - Pantalla "ver cobro" (`/cobros/[id]`) convertida a burdeo/texto blanco; nuevo componente compartido `src/components/boton-volver.tsx` (`BotonVolver`, `router.back()`) para "Volver" en páginas que son Server Component.
   - **Regla de negocio nueva: el arriendo se paga por adelantado** → `periodoArriendoVigente()` en `src/features/cobros/queries.ts` calcula el período a generar/revisar como el mes **siguiente** al actual, no el mes calendario en curso (aplicado al aviso "sin arriendo generado", el período por defecto de "Generación asistida", y la tarea del Dashboard). Liquidaciones no se tocó (revisa lo ya cobrado, es otro concepto). La primera tarjeta de Cobros ahora también muestra cuántas propiedades tienen arriendo sin generar y la deuda **proyectada** (canon vigente de esos contratos, no el saldo de cargos ya generados).
   - **Bug de zona horaria real, encontrado por feedback de Eduardo** (la tarjeta seguía mostrando julio en vez de agosto): `periodoArriendoVigente()` construía la fecha del mes siguiente y llamaba `.toISOString()` — esa conversión a UTC puede retroceder un día (y por lo tanto un mes) en una zona horaria con offset negativo respecto a UTC como Chile. Reescrito con aritmética de meses pura (`getFullYear()*12 + getMonth()`), sin pasar por `Date`/UTC en ningún punto. **Lección**: cualquier cálculo de "período siguiente" en este proyecto debe evitar `new Date(...).toISOString()` para derivar mes/año — usar aritmética entera.
3. **`8c8be69`** — Cuatro cambios grandes en la misma sesión:
   - **Fechas en `dd/mm/yyyy` en toda la app**: nueva utilidad `src/lib/fecha.ts` (`formatearFecha`, `formatearPeriodo`), aplicada en ~25 lugares que mostraban fechas ISO crudas (Cobros, Contratos, Gastos, Liquidaciones, Documentos, incluyendo los `Campo`/`Dato` de solo lectura en Contratos/Propiedades).
   - **"Registrar pago" → wizard**: se quita el formulario inline de `/cobros/[id]`; nueva ruta `/cobros/[id]/pagos/nuevo` con `src/features/cobros/pago-wizard.tsx` (mismo patrón: preguntas, overlay de Cancelar, borrador no aplica porque no tiene sentido para un pago puntual). Se eliminó `registrar-pago.tsx` (reemplazado, sin otros usos).
   - **Filtros → combobox**: los `<select>` de Propiedad/Propietario/Arrendatario en los 5 filtros nuevos (Cobros, Liquidaciones, Gastos, Documentos, Reportes) pasan a `ComboboxOpcion`, igual que Región en Propiedades.
   - **Cobros — acordeón por período**: el listado de cargos ya no es una grilla plana de tarjetas; se agrupa por período (mes/año) en un `<details>` — "Deuda pendiente" a la izquierda, monto del período + mes/año a la derecha, y al desplegar los cargos individuales quedan separados por una línea fina (`divide-white/10`), manteniendo "ver más información"/ícono "ver"/badge de estado igual que antes. "Generación asistida" ahora solo se muestra si hay contratos sin arriendo generado para el período (antes siempre visible).
4. **`dd2eec8`** — El botón "+" circular de "Registrar pago" (nuevo en el punto anterior) se veía mal ("feo" — feedback textual de Eduardo); se reemplazó por un botón de **ancho completo** de la sección, mismo estilo burdeo/texto blanco que las tarjetas de acordeón.

### Resumen de la primera mitad de esta sesión (`ae74a4a`→`85c0f0a`→`fb5afbc`→`bb5c404`, ya documentado, sin repetir detalle)

Botón atrás generalizado (sube un nivel en la ruta); sistema de reajuste de canon vía UF con revisión manual (nunca automático — `canon_actual`/`canon_uf_base`/`fecha_proximo_reajuste`, migraciones `0021`–`0023`); menú completo activado + patrón de diseño replicado en los 5 módulos que estaban ocultos; primera corrección de formato (tarjetas, `ComboboxOpcion`, overlay de cancelar — con el bug de la primera pasada, corregido en la segunda mitad arriba).

### Resumen de lo hecho la sesión previa 2026-07-27 (5 commits, en orden)

1. **`0bb6edb`** — Ajustes de campos en Propietarios/Arrendatarios (detalle + creación), feedback de probar en el celular:
   - RUT con máscara en vivo (`formatearRut` en `lib/rut.ts`, `12.345.678-9`), en RUT y RUT del titular.
   - Banco: de texto libre a `Combobox` con catálogo `data/bancos.ts`.
   - N° de cuenta: filtra a solo dígitos (`formatearNumeroCuenta`), se guarda como texto (no `type=number`, para no perder ceros a la izquierda).
   - "Titular de la cuenta" → label acortado a "Titular" (el `name="titular_cuenta"` no cambió).
   - Email: `type=email` + validación de estructura (`esEmailValido` en `lib/contacto.ts`) en wizard, detalle y servidor; en lectura se renderiza con `<wbr/>` antes del `@` (`EmailTexto`) para que el salto de línea no se sobreponga al teléfono.
   - Teléfono: `type=tel` + filtro a dígitos y un único `+` inicial (`formatearTelefono`), sin formato rígido de agrupamiento.
   - Botones del wizard: fila `grid grid-cols-3` (Cancelar/Atrás/Siguiente en posiciones fijas).
   - Confirmación de "Cancelar" del wizard: de tarjeta inline a **overlay fijo** (`fixed inset-0`) centrado sobre toda la pantalla.
2. **`4fd0ab1`** — **Contratos**, patrón de diseño replicado (tarjetas + filtros estado/activo, detalle con edición en línea, wizard de creación, eliminar con validación previa). Decisiones de alcance documentadas en `docs/DISENO_PAGINAS.md` (sección "Contratos"): Propiedad se edita con `<select>` nativo (no `Combobox`, por id≠label); arrendatarios del contrato embebidos en el mismo `/contratos/[id]` (sin ruta `/editar` aparte); el wizard de creación no pide arrendatarios (se asignan después, igual que la pantalla vieja). Se eliminó `contrato-form.tsx` (reemplazado). **Bug real encontrado de paso y corregido en los 3 wizards existentes**: el input de la pregunta *activa* nunca tenía `name`, así que el navegador no lo mandaba en el `FormData` — la última pregunta de cada wizard nunca se guardaba (`rut_titular`/`numero`/`observaciones`, pasaba desapercibido por ser campos opcionales). Detalle técnico en `docs/DISENO_PAGINAS.md`.
3. **`b0d663b`** — Tres pedidos puntuales: (a) botón atrás del celular (Android) ahora **siempre vuelve a `/dashboard`** en cualquier página — decisión de producto explícita, no un bug (`src/components/back-to-dashboard.tsx`, trampa de historial con `pushState`/`popstate`; efecto secundario aceptado: salta la confirmación de "Cancelar" del wizard si se usa a mitad de uno, sin pérdida de datos porque el borrador sigue en `localStorage`); (b) primer intento de unificar el select de "Tipo de cuenta" con `select-styled.tsx` (select nativo + chevron); (c) Dashboard con tema burdeo nuevo + 3 indicadores nuevos (Ocupación %, Propietarios activos, Arrendatarios activos).
4. **`84f6718`** — El intento (b) de arriba **no fue suficiente**: un `<select>` nativo en el celular sigue abriendo el picker del sistema operativo sin importar el CSS. Eduardo pidió tomar Región/Comuna como referencia real. Se creó `src/components/select-combo.tsx` (mismo comportamiento que `Combobox` — lista propia, sin picker nativo — pero soporta pares `{value, label}`, necesario porque el label mostrado no es el value guardado en el enum de Postgres). Aplicado a Tipo de cuenta en detalle y wizard de Propietarios. `select-styled.tsx` queda solo para Tipo de persona (2 opciones, sin el mismo reclamo).
5. **`db46805`** — Tarjetas KPI del Dashboard eran demasiado altas (ícono arriba + bloque de texto apilado abajo); se comprimieron a una sola fila (ícono + valor/label + flecha), angostando la tarjeta.

También pendientes de ejecutar (dependientes de que Eduardo confirme fechas/datos, no urgentes):
- `supabase/maintenance/limpiar_datos_prueba_2026-07-24.sql` (antes de cargar datos reales)
- `supabase/maintenance/cargar_datos_reales_803_1907A.sql` (carga a Eduardo como propietario + las 2 propiedades reales + Jimmy/Paul + contratos directos — tiene 3 campos marcados "AJUSTAR" antes de correr)

### Modalidad "Administrador de Contratos de Arriendo" — conocimiento cerrado, transición operativa

Contrato base propio redactado y cerrado (`docs/CONTRATO_BASE_ARRIENDO.md`, v2). Detalle completo del análisis de los 2 contratos AssetPlan (803, 1907-A), decisiones de negocio y transición (finiquitos, garantías traspasadas) en **`docs/ADMINISTRADOR_CONTRATOS_ARRIENDO_SESION_2026-07-24.md`** — ese documento tiene su propia lista de pendientes sin cerrar (firma de finiquitos, confirmación de razón social AssetPlan, papel de garantía de la encimera en 803, recepción de $895.000). Revisarlo antes de ingresar esos 2 contratos como datos reales.

### Rediseño de UI — nueva identidad visual + patrón de páginas

Nueva etapa de la app (uso con datos reales, no solo demo): tema oscuro grafito + burdeo (antes blanco), y un **patrón de página unificado** documentado en **`docs/DISENO_PAGINAS.md`** + **`docs/PAGINAS_NUEVAS.md`** (checklist rápido — léelos primero si vas a tocar cualquier página de listado/detalle/creación):

- **Tarjetas de listado** burdeo (no blancas ni `<table>`), badges de estado con fondo sólido, disclosure "Ver más información" para datos secundarios, switch on/off real (si aplica), filtros colapsables detrás de un ícono (arrancan ocultos, "Limpiar" solo resetea el formulario sin navegar — "Aplicar" es quien ejecuta el filtro).
- **Detalle de un registro**: panel burdeo único, edición **campo por campo en el mismo grid** al presionar "Editar" (nunca un formulario aparte ni modal — salvo Gastos, que mantiene `/editar` aparte por la regla de bloqueo tras liquidar), botón "Eliminar" que se auto-valida contra relaciones bloqueantes ANTES de ofrecerse, confirmación propia (no `confirm()` nativo), "Volver" con `router.back()` (preserva filtros de la pantalla de origen).
- **Creación**: wizard de una pregunta por pantalla (Atrás/Siguiente, sin "Omitir"), con borrador persistente en `localStorage` (sobrevive si el usuario sale de la app a medio llenar), preguntas condicionales según respuestas previas, y confirmación propia al cancelar (overlay fijo arriba de pantalla en los wizards nuevos; los 4 originales aún con el overlay inline viejo — ver deuda anotada arriba).
- Selectores de entidad (id≠label, ej. Propiedad/Propietario) usan `ComboboxOpcion` (buscador, fondo blanco) — no `<select>` plano.
- **Replicado en los 9 módulos**: Propiedades (referencia original), Arrendatarios, Propietarios, Contratos, Cobros, Liquidaciones, Gastos, Documentos, Reportes. Menú completo activado (2026-07-29).

**Detalle línea por línea de todo lo hecho** (útil solo si se necesita el porqué de una decisión puntual): ver `## Últimos Cambios` más abajo, entradas del 26-jul-2026 en adelante — hay ~20 commits pequeños e iterativos con el feedback real de Eduardo probando en el celular (colores, alineaciones, textos). No hace falta leerlos todos para continuar; `docs/DISENO_PAGINAS.md` ya consolida las decisiones finales.

**Documentos de contexto:**
- `PROYECTO.md` (este archivo) — memoria viva del proyecto. Contiene el roadmap oficial de Hardening (T1–T22) y el Backlog QA 1 con reglas de negocio oficiales (ver secciones más abajo, sin cambios esta sesión).
- `docs/DISENO_PAGINAS.md` — patrón de diseño de páginas (fuente de verdad para UI).
- `docs/PAGINAS_NUEVAS.md` — checklist rápido de 6 puntos para migrar/crear una página sin dejar piezas afuera (tarjetas, combobox, overlay de cancelar, componentes con variante clara/oscura, filtro colapsable, no tocar la lógica de negocio).
- `docs/CONTRATO_BASE_ARRIENDO.md` + `docs/ADMINISTRADOR_CONTRATOS_ARRIENDO_SESION_2026-07-24.md` — modalidad de contratos de arriendo.
- `../RZK.md` (en la carpeta `Ruzosky/`, fuera del proyecto) — playbook transversal OPT-IN; solo se usa si se solicita explícitamente.

**Pendiente / próximo (en orden):**
0a. **Pushear el commit `2527292` (nombre de cargo) a `origin/main`** — listo con `tsc`/`build` verdes, falta solo confirmación explícita de Eduardo para el push (no se hace automático). Sin esto el cambio no llega a producción (Vercel despliega desde `origin/main`).
0b. **Probar en producción real** (celular de Eduardo) que editar un cargo y ponerle nombre lo guarda, se ve en el detalle junto a la dirección, y aparece también en la tarjeta de listado de `/cobros`. Ningún cambio de esta sesión se verificó en navegador real (sin Playwright/chromium-cli en el entorno).
0. **Re-probar el flujo de solicitudes de pago con comprobante** (esta vez con el fix definitivo de submit implícito, commit `3f71454`):
   - Rehacer/editar la solicitud de Paul Onetto ($132.614) que quedó sin comprobante guardado por el bug ya corregido — confirmar que esta vez sí queda guardado y visible en "Ver comprobante" desde `/cobros/solicitudes`.
   - Confirmar que "Editar pago informado" (edición de solicitud pendiente, migración `0033`) funciona de punta a punta: cambiar monto/fecha/medio/referencia y adjuntar o reemplazar el comprobante.
   - Probar `/recuperar-clave` → `/actualizar-clave` (restablecer contraseña desde login) — **verificar primero si el proyecto de Supabase tiene SMTP configurado**, si no el correo de recuperación nunca va a llegar.
1. **Pendiente sin resolver — posible bloqueo de capturas de pantalla en el celular de Eduardo**: reportó no poder tomar captura de RZK Prop (sale en negro). Se le pidió confirmar si el bloqueo es específico de la app (imposible vía código — ninguna PWA puede activar `FLAG_SECURE`) o viene del dispositivo (Carpeta segura, perfil de trabajo/MDM, navegador). Sin respuesta aún.
2. **Seguir probando el resto del flujo end-to-end del Portal** (invitación, alta express, completar-perfil, cargos desfazados, aprobar/rechazar solicitudes) — ya se probó y corrigió una parte importante esta sesión (ver resumen arriba), falta confirmar el resto.
3. Confirmar que "Eliminar" funciona en producción en Contratos (`0020_contratos_delete.sql` ejecutada).
4. Probar el flujo de reajuste de canon con datos reales cuando llegue el 01-09-2026 (próxima revisión real de Vicuña Mackenna).
5. Roadmap de Hardening — Sprint 1: **T5 (gate de rol) y T11 (regenerar tipos) ya completados.** Quedan T2 (auditoría) y T3 (backups).
6. Backlog QA (R1/R2/R3/R5, motor de liquidación) — sin cambios, sigue pendiente.
7. Evaluar correr `limpiar_datos_prueba` + `cargar_datos_reales_803_1907A` para empezar a usar la app con datos reales (todas las migraciones hasta `0033` están aplicadas).
8. Corregir a mano en la BD los cargos de luz/agua/internet creados antes de esta sesión que quedaron guardados con `tipo_cargo = 'otro'` por el bug ya corregido (ver resumen de sesión arriba).
9. **Lecciones técnicas de esta sesión a tener presentes siempre**:
   - **Nunca confiar en interceptar la tecla Enter (`onKeyDown`) para evitar un submit implícito de formulario en un wizard móvil** — no es confiable entre teclados/navegadores. La forma correcta: exigir una marca explícita puesta solo por la acción real de guardar, y bloquear el submit en el `onSubmit` del `<form>` si esa marca no está puesta (ver `permitirSubmit` en `pago-wizard.tsx`/`solicitud-wizard.tsx`).
   - Antes de asumir que algo "se ve como lentitud" viene de un patrón específico (ej. `autoFocus` en wizards), confirmar con el usuario **dónde exactamente** lo nota — se perdió una vuelta completa asumiendo mal el alcance del problema.
   - Una PWA no puede bloquear ni permitir capturas de pantalla — eso es exclusivo de apps nativas (`FLAG_SECURE`); si un usuario reporta ese síntoma, es del dispositivo/navegador, no del código.
   - Nunca derivar mes/año con `new Date(...).toISOString().slice(...)` server-side (offset UTC negativo en Chile) — usar aritmética entera de meses.
   - Nunca compartir un link de un solo uso (magic link/invite) directo por un canal con preview automático (WhatsApp, Slack, etc.) — siempre interponer una página de aterrizaje pública que requiera un click humano antes de consumir el token.
   - Nunca agregar una policy RLS que consulte una tabla cuyas propias policies consulten de vuelta a la tabla original — usar una función `security definer` para romper el ciclo (ver `propiedad_de_contrato()` en `0028`).

**Flujo de trabajo:** construir → `next build` verde + `tsc` limpio → commits locales pequeños por entregable → **push solo con autorización explícita** de Eduardo (dice "push"/"aplica commit y push"). Migraciones: Eduardo las aplica en el SQL Editor y confirma — nunca asumir que ya corrieron.

**Reglas activas:** español chileno; scopes personales (no org empresarial) en GitHub/Supabase/Vercel; identidad git local = Laleke; `@supabase/ssr` alineado con `supabase-js`; toda tabla nueva con `empresa_id` + RLS solo-admin **incluyendo política DELETE si el borrado real es parte del diseño** (lección de esta sesión); **app centrada en la Propiedad** (todo lo derivable se deriva, no se pide al usuario).

## Arquitectura
Serverless, modular, multitenant. Frontend en Vercel; backend, BD, auth y storage en Supabase.

### Frontend
- **Next.js 15** (App Router) + **React 19** + **TypeScript** + **Tailwind CSS 4**.
- Estructura `src/`. PWA vía **Serwist** (`@serwist/next`) — service worker en `src/app/sw.ts`.
- Organización por *features* (módulos de negocio aislados) bajo `src/features/`.

### Backend
- **Supabase** como único backend. Sin servidor propio, sin VPS, sin FTP.
- Acceso a datos vía `@supabase/ssr` (3 clientes: browser, server components, middleware).
- Lógica que requiera privilegios → Supabase Edge Functions o RPC (Postgres functions), no servidor Express.

### Base de Datos
- **PostgreSQL** (Supabase). Aislamiento multitenant por **Row Level Security (RLS)**.
- Cada tabla de negocio lleva `empresa_id`. Las políticas RLS filtran por el tenant del usuario autenticado.
- Migraciones versionadas en `supabase/migrations/`.

### Integraciones
- Hosting: **Vercel**. Repositorio: **GitHub**. Storage: **Supabase Storage**.
- <!-- pendiente: pasarela de pagos, notificaciones (email/WhatsApp) — no definidas aún -->

## Reglas de Negocio
<!-- pendiente: se definen al construir cada módulo -->

## Flujo Operacional
Usuario autentica con Supabase Auth → middleware refresca sesión y protege rutas → su `profile` define `empresa_id` y `rol` → RLS limita los datos a su empresa → la UI muestra el dashboard según rol.

**Bootstrap inicial (primer admin/empresa):** se resuelve con `supabase/bootstrap_admin.sql`, ejecutado manualmente UNA vez en el SQL Editor (corre como `service_role`, único punto donde se bypassa RLS de forma autorizada). Requiere crear antes el usuario en Authentication > Users. El script es atómico, idempotente y aborta si el usuario auth no existe.

## Decisiones Técnicas
- **Multitenancy por RLS + `empresa_id`** (2026-06-27): una sola BD, aislamiento por fila. Motivo: cumple "multiempresa sin reescritura" sin la complejidad de schema/DB-per-tenant; compatible con free tier. Impacto: toda tabla de negocio debe llevar `empresa_id` y políticas RLS desde su creación.
- **Roles en tabla `profiles`, no en Auth** (2026-06-27): `auth.users` solo identifica; `profiles` define `empresa_id` y `rol`. Motivo: seguridad real validada en BD, no en frontend.
- **PWA con Serwist** (2026-06-27): reemplaza `next-pwa` (abandonado, incompatible con App Router).
- **`@supabase/ssr`** (2026-06-27): reemplaza `auth-helpers` (deprecado).
- **Garantía como historial de movimientos, no un monto único** (2026-08-17, confirmado con Eduardo antes de implementar): tabla `contrato_garantias` (recepción/retención/devolución) en vez de una columna `monto_garantia` en `contratos`. Motivo: permite retenciones parciales y devolución trazable; el saldo disponible se calcula siempre en la query, nunca se persiste (evita desincronización). Embebida en `contratos/[id]` como sub-recurso (sin módulo ni nav propios), mismo patrón que "Arrendatarios del contrato".
- **Incidencias con proveedor y seguimiento completo, no un registro simple** (2026-08-17, confirmado con Eduardo antes de implementar): módulo de nivel superior con estados (`reportada→agendada→en_proceso→resuelta/cancelada`), datos de proveedor, y vínculo opcional 1:1 a un Gasto (botón manual "Generar gasto", no un proceso batch de "claim" como Gastos↔Liquidaciones).
- **Tablas nuevas con RLS solo-admin y `empresa_id` propio, nunca `exists (select ... from contratos/propiedades ...)` inline en la policy** (regla dura, reafirmada 2026-08-17 al agregar `contrato_garantias`/`incidencias`): evita repetir el incidente de recursión RLS que tumbó producción dos veces (`0028`/`0034`, ver `## Riesgos Activos`). Si en el futuro una tabla nueva necesita scoping por propietario/arrendatario (no solo admin), envolver cualquier lectura a `contratos`/`propiedades` en una función `security definer` (patrón `propiedad_de_contrato`), nunca inline.

## Decisiones Rechazadas
- **Schema-per-tenant / DB-per-tenant**: sobreingeniería para 1 empresa; rompe free tier. Reemplazado por RLS.
- **`next-pwa`**: abandonado e incompatible con App Router. Reemplazado por Serwist.
- **Backend Express propio**: viola la restricción de "Supabase único backend". Lógica privilegiada → Edge Functions / RPC.

## Supuestos Operacionales
- 1 empresa activa inicialmente; diseño soporta N sin reescritura.
- Usuarios concurrentes bajos al inicio (uso interno de corredora).
- Costo objetivo inicial: $0 (free tiers de Supabase + Vercel).

## Restricciones de Cuenta / Hosting
- **Scope personal obligatorio**: GitHub, Supabase y Vercel deben usar las cuentas **personales** de Eduardo, NO la organización empresarial. Eduardo tiene cuenta empresa en estas plataformas; mezclar este proyecto la expondría a toda la org.
  - GitHub: repo privado en cuenta personal.
  - Supabase: organización propia de un solo miembro.
  - Vercel: cuenta personal (Hobby), no el Team.
- **Nunca `git push` ni configurar remoto de forma automática**: los commits locales se hacen según se pidan, pero el envío a GitHub lo ejecuta Eduardo manualmente con sus credenciales personales.

### Regenerar `database.types.ts` (comandos a mano — PowerShell de Eduardo)

Eduardo no tiene el CLI de Supabase instalado global, solo vía `npx` bajo demanda; además su PowerShell bloquea los wrappers `.ps1` de `npx`/`npm` por política de ejecución (error "no se puede cargar... la ejecución de scripts está deshabilitada"). Comandos que sí funcionan, en este orden, cada vez que haya una migración nueva que agregue/cambie columnas o enums:

```powershell
npx.cmd supabase login
npx.cmd supabase gen types typescript --project-id rrhhnfbxrxceedgueyss > src/types/database.types.ts
```

- `npx.cmd`/`npm.cmd` (no `npx`/`npm` a secas) evita el bloqueo de execution policy — no requiere cambiar la política de seguridad del sistema.
- `supabase login` abre el navegador para autorizar; es interactivo, lo corre Eduardo, no queda guardado para reutilizar desde otra sesión/máquina.
- `--project-id rrhhnfbxrxceedgueyss` evita el paso de `supabase link` (que pide vincular el proyecto local); el project ref sale de `NEXT_PUBLIC_SUPABASE_URL` (`https://rrhhnfbxrxceedgueyss.supabase.co`).
- El archivo resultante suele quedar en **UTF-16** por cómo PowerShell maneja la redirección `>` — hay que convertirlo a UTF-8 después (`iconv -f UTF-16LE -t UTF-8`) o el build falla. Además, la versión actual del CLI de Supabase (2.110+) ya no emite los alias de enum de nivel superior (`RolUsuario`, `CategoriaDocumento`, etc.) que gran parte del código importa — hay que restaurarlos a mano al final del archivo como `export type X = Database["public"]["Enums"]["x"]` (bloque ya presente en el archivo actual; solo hay que volver a pegarlo si `types:gen` lo pisa).
- **Hallazgo 2026-08-17 — Claude puede correrlo directo, sin pedírselo a Eduardo**: ejecutando `npx.cmd supabase gen types typescript --project-id rrhhnfbxrxceedgueyss` desde la tool Bash (no PowerShell) en este entorno, el comando funcionó de punta a punta **sin pedir `supabase login`** (sesión ya autorizada quedó disponible para el CLI) y **sin el problema de UTF-16/BOM** (Bash no tiene el mismo comportamiento de redirección `>` que PowerShell — el archivo salió en UTF-8 plano, sin conversión). Confirmado con un diff línea a línea contra la edición manual previa: coincidieron exactas. **Para la próxima migración nueva**: probar primero `npx.cmd supabase gen types ...` directo vía Bash antes de asumir que hace falta pasárselo a Eduardo o editar `database.types.ts` a mano — solo recurrir a la edición manual si el comando falla (ej. sesión de login expirada).

## Base de Datos
### Tablas Principales
- `empresas` — tenants del SaaS.
- `profiles` — extiende `auth.users`; ligа usuario a `empresa_id` y `rol` (admin/propietario/arrendatario).
- `propietarios` — dueños de propiedades. Persona natural o jurídica; datos de contacto y bancarios para liquidaciones. `unique(empresa_id, rut)`. Baja lógica (`activo`). RLS admin + (2026-07-30) RLS de solo lectura para el propio propietario. **Portal (migración `0024`):** `profile_id` (nullable, único, liga a `profiles.id`), `estado_invitacion` (enum `sin_invitar/invitado/activo`), `invitado_en`/`invitado_por`.
- `propiedades` — inmuebles. Tipo/estado/moneda como enums; `valor_referencial_arriendo` (el canon real va en contrato), `publicada` (portales) ≠ `estado`. `codigo_interno` **autogenerado y no editable** (comuna+tipo+correlativo); `unique(empresa_id, codigo_interno)`. `direccion` nullable (permite guardar incompleta; mínimo para crear = comuna+tipo). Baja lógica. RLS solo admin. NO tiene `propietario_id`: la relación vive solo en la tabla puente.
- `propietarios_propiedades` — tabla puente N:M, única fuente de verdad de la relación. `porcentaje_participacion` (copropiedad, check 0–100), `unique(propiedad_id, propietario_id)`. Permite DELETE (desasignar). RLS solo admin.
- `arrendatarios` — inquilinos. Misma forma que propietarios pero sin datos bancarios. Reutiliza enum `tipo_persona`. `unique(empresa_id, rut)`. Baja lógica. RLS admin + (2026-07-30) RLS de solo lectura para el propio arrendatario. **Portal (migración `0024`):** mismas columnas `profile_id`/`estado_invitacion`/`invitado_en`/`invitado_por` que `propietarios`.
- `contratos` — núcleo del negocio. 1 propiedad + canon (CLP/UF) + reajuste + comisión + administración + estado. `numero_contrato` editable único por empresa. Baja lógica. RLS solo admin. **Campos de reajuste (2026-07-29, migraciones `0021`–`0023`):** `canon_actual` (numeric, vigente/cobrado; `canon_monto` queda como el original), `canon_uf_base` (numeric, monto fijo en UF cuando `reajuste_tipo='UF'`, habilita "Aplicar reajuste"), `fecha_proximo_reajuste` (date, próxima fecha a revisar — nunca se aplica sola).
- `contratos_arrendatarios` — tabla puente N:M contrato↔arrendatarios (matrimonios/codeudores/representantes). `unique(contrato_id, arrendatario_id)`. DELETE permitido. RLS admin + (2026-07-30, migración `0027`) SELECT para el propietario dueño de la propiedad del contrato (para mostrar el nombre del arrendatario en su propia vista de contrato — faltaba en la migración `0024` original).
- `cargos` — deuda generada por contrato/período/tipo. `unique(contrato_id, periodo, tipo_cargo)`. `saldo_pendiente` y `estado` recalculados por la app. RLS solo admin.
- `pagos` — abonos sobre un cargo (soporta pago parcial). `medio_pago` enum. RLS solo admin. **`documento_id`** (2026-07-30, migración `0025`, mismo patrón que `gastos.documento_id`): comprobante opcional, adjuntable durante el wizard "Registrar pago" o después desde el detalle del cargo.
- `liquidaciones` — monto a transferir al propietario por período. Estados pendiente/pagada/anulada; subtotales + total; registro de pago (fecha/observación) + `comprobante_url` (estructura futura). Único por (empresa, propietario, período) salvo anuladas. RLS solo admin.
- `liquidacion_detalles` — líneas (ingreso/descuento) con `concepto`, `referencia_tipo`/`referencia_id` (trazabilidad a cargo/contrato). RLS solo admin.
- `auditoria` — eventos genéricos (usuario, acción, entidad, datos jsonb). RLS solo admin. Helper `src/lib/auditoria.ts`.
- `documentos` — Centro Documental. Metadatos (`nombre`, `categoria` enum, `observaciones`, `fecha_documento`, `subido_por`/`subido_por_email`) + relaciones **opcionales** (`propietario_id`/`arrendatario_id`/`propiedad_id`/`contrato_id`, `on delete set null`) + `version_actual` (puntero). RLS solo admin. Índices por empresa/categoría/relación/fecha.
- `gastos` — fuente oficial de gastos. `propiedad_id` obligatorio; relaciones opcionales a contrato/propietario/arrendatario/liquidación/documento. `responsable_pago` (propietario/arrendatario/corredora), `estado` (pendiente/pagado/anulado), `descontar_de_liquidacion` (afecta rentabilidad del dueño), `liquidacion_id` (dónde se descontó; null = pendiente). RLS solo admin. Consumida por Reportes y por el **descuento automático en liquidaciones** (se reclama con `estado→pagado` + `liquidacion_id`, se libera al anular).
- `documento_versiones` — cada archivo subido de un documento. `storage_path` (bucket privado `documentos`), `nombre_archivo`, `tamano_bytes`, `mime_type`, `version`, `unique(documento_id, version)`, `on delete cascade`. RLS solo admin. El aislamiento del bucket también se refuerza con políticas RLS sobre `storage.objects` (path `<empresa_id>/<uuid>.<ext>`).
- `solicitudes_pago` (2026-07-30, migración `0026`) — el arrendatario reporta un pago desde el portal; queda `pendiente` hasta que el propietario (o el admin) lo aprueba o rechaza. Campos espejo de `pagos` (monto/fecha/medio/referencia/observaciones) + `comprobante_*` (archivo suelto en Storage, no un `documento` todavía — se promueve a uno real recién al aprobar) + `estado`/`motivo_rechazo`/`pago_id` (el pago real creado al aprobar)/`revisado_por`/`revisado_en`. **Única tabla con INSERT no-admin en todo el esquema** (arrendatario inserta la suya propia) — ver nota en Seguridad. **`excede_saldo`/`saldo_pendiente_al_crear`** (2026-07-31, migración `0030`): el monto informado NO se bloquea si supera el saldo pendiente del cargo (puede ser un abono adelantado o un error a corregir) — se deja pasar, pero se marca (snapshot del saldo al momento de crear, puede diferir del saldo real al momento de revisar) para que el propietario/admin lo revise con más cuidado antes de aprobar.
- `profiles.password_set` (2026-07-31, migración `0031`) — rastrea si el usuario del portal ya definió su propia contraseña. Sin esto, alguien que entra por primera vez con el link de invitación queda con sesión válida en ESE navegador pero sin forma de volver a entrar desde otro dispositivo (nunca completó `/portal/set-password`) — bug real encontrado el mismo día. Backfill: los que ya estaban `activo` antes de la migración se asumen con contraseña ya puesta.

### Relaciones
- `profiles.id` → `auth.users.id` (1:1). `profiles.empresa_id` → `empresas.id` (N:1).
- `propietarios.empresa_id`, `propiedades.empresa_id` → `empresas.id` (N:1).
- `propietarios_propiedades`: N:M entre `propietarios` y `propiedades` (FKs con `on delete cascade`). Es la **única** vía de la relación; `propiedades` no referencia propietarios directamente.

### Reglas de Negocio
- La participación total de copropietarios en una propiedad no puede superar 100%. Se valida en la acción de asignación (no por constraint de fila); cada fila individual valida 0 < % ≤ 100 por check.
- **Sincronización contrato→propiedad (contrato = fuente de verdad), en capa de aplicación:**
  - contrato `vigente` o `renovado` → `propiedad.estado = arrendada`.
  - contrato `terminado` → si no queda otro contrato activo (vigente/renovado) en la propiedad → `propiedad.estado = disponible`.
  - contrato `vencido` o `borrador` → no toca la propiedad (en Chile el arrendatario suele seguir ocupando tras el vencimiento).
- **Restricción:** no se permite poner un contrato en `vigente` si la propiedad ya tiene otro contrato activo (validado contra los contratos, no contra `propiedad.estado`).
- Reajuste IPC/UF exige `periodicidad_reajuste_meses` > 0. Si hay `tipo_comision`, exige valor. Si `cobra_administracion`, exige monto o porcentaje.
- **Cobros (cargos + pagos):** la deuda (cargo) existe antes del pago. Un pago abona a un cargo; `saldo_pendiente = monto − Σ pagos`; `estado` = pagado (saldo≤0) / parcial (hubo abonos) / pendiente. **`vencido` se deriva en lectura** (saldo>0 y `fecha_vencimiento < hoy`), no se almacena (evita cron). No se permite pago que supere el saldo. Generación de arriendos del mes es **asistida por botón** (no cron); upsert idempotente por `(contrato, periodo, tipo)`; usa `canon_actual` (no `canon_monto`) desde 2026-07-29.
- **El arriendo se paga por adelantado (2026-07-29):** el período "vigente" para generar/avisar arriendos es el mes **siguiente** al calendario actual, no el mes en curso — `periodoArriendoVigente()` en `src/features/cobros/queries.ts`, usado en el aviso "sin arriendo generado" de Cobros, el período por defecto de "Generación asistida" y la tarea del Dashboard. **No aplica a Liquidaciones** (revisa lo ya cobrado del mes en curso, no lo por-adelantar). Implementado con aritmética entera de meses, nunca `Date`/`toISOString` (ver Deuda Técnica / lección de zona horaria).
- **Reajuste de canon (2026-07-29, ver `contratos`):** `canon_monto` es el original pactado, inmutable de hecho; `canon_actual` es el vigente (el que se cobra). **Nunca se aplica solo** — el admin decide explícitamente por contrato: "Aplicar reajuste (UF)" (calcula `canon_uf_base × UF del corte trimestral vigente` vía mindicador.cl y avanza `fecha_proximo_reajuste`) o "Postergar N meses" (solo corre la fecha). Se descartó un cron que aplicara solo tras comprobar con datos reales que cada contrato heredado (AssetPlan) tiene su propia cadencia real (uno trimestral en la práctica, otro anual) — aplicar una regla única a todos habría reajustado alguno antes de tiempo. `fecha_proximo_reajuste` vencida aparece como aviso (no bloqueante) en Cobros al generar arriendos y como tarea pendiente en el Dashboard.
- **Portal propietario/arrendatario (2026-07-30, T20, migración `0024`):** alta de cuenta **siempre manual, iniciada por el admin** desde la ficha de propietario/arrendatario ("Invitar al portal") o alta express solo con email+teléfono (`invitarNuevo`, la persona completa su propia ficha en `/portal/completar-perfil`); nunca auto-registro. El link de invitación se envía por WhatsApp (nunca email automático). Cada rol ve únicamente lo propio, resuelto vía `profile_id` en `propietarios`/`arrendatarios` y las tablas puente existentes — nunca por email ni por búsqueda libre. ~~El portal es 100% de solo lectura~~ — ver excepción de "Solicitudes de pago" abajo (única escritura permitida).
- **Solicitudes de pago (2026-07-30, migración `0026`):** el arrendatario puede "Informar pago" desde `/portal/cargos` (mismo set de preguntas que el wizard admin de "Registrar pago": monto/fecha/medio/comprobante) — pero esto **no crea un `pago` real**. Queda `pendiente` hasta que el **propietario dueño de esa propiedad, o el admin**, la aprueba o rechaza (`/portal/solicitudes` y `/cobros/solicitudes` respectivamente). Al aprobar, se crea el `pago` real (mismo efecto que `registrarPago`) y se recalcula el cargo; el comprobante adjunto (si lo hay) se promueve recién ahí a un `documento` real. Al rechazar, queda registrado el motivo — el arrendatario lo ve en su portal y puede volver a solicitar. **Es la única escritura (INSERT) que un rol no-admin tiene en todo el esquema** — deliberado, con su propia política RLS `solicitudes_pago_insert_arrendatario` acotada a su propio contrato (no vía cliente admin, a diferencia del resto del portal). El monto informado **no se bloquea** si supera el saldo pendiente (puede ser abono adelantado o error a corregir) — se marca (`excede_saldo`) para revisión más cuidadosa al aprobar (2026-07-31, migración `0030`).
- **Edición de solicitud de pago pendiente (2026-08-02, migración `0033`):** el arrendatario que creó una solicitud puede editarla (monto/fecha/medio/referencia/comprobante) **mientras siga `pendiente`** — nueva policy RLS de UPDATE acotada a `estado = 'pendiente'` y a su propio `arrendatario_id`, con el mismo `with check` para que ni siquiera pueda cambiar el estado él mismo. Antes esta acción quedaba oculta sin alternativa una vez informado el pago.
- **Cargos "desfazados" (2026-07-31, migración `0032`):** gasto_comun/luz/agua/internet tienen un período de **consumo** real (ej. boleta de luz 06/06–06/07) que casi nunca coincide con el mes en que se **cobra/vence** — `periodo` en `cargos` sigue siendo siempre el mes de cobro (consistente con el resto del sistema, que organiza todo por caja/vencimiento, no por consumo); el rango de consumo real se guarda aparte como fechas estructuradas (`fecha_consumo_desde`/`fecha_consumo_hasta`, no texto libre en observaciones — decisión explícita para no depender de que alguien lo escriba bien). `TIPOS_DESFAZADOS` en `src/features/cobros/constants.ts` define qué tipos aplican. **Regla clave**: al terminar un contrato (`estado='terminado'`), casi siempre falta generar el último cargo desfazado (el consumo sigue corriendo hasta que el arrendatario se va) — nueva tarea pendiente en Dashboard + banner en Cobros ("contrato terminado con cargo desfazado sin generar") para no perderlo de vista antes de devolver la garantía; se resuelve solo generando ese último cargo (sin botón de "marcar resuelto", decisión explícita de Eduardo).

### Índices Relevantes
- `profiles(empresa_id)`, `profiles(rol)`, `propietarios(empresa_id)`.

### Modelo de datos — convenciones decididas
- **RUT** se guarda normalizado `cuerpo-dv` (sin puntos), validado con dígito verificador en la frontera (`src/lib/rut.ts`). Único por empresa, no global.
- **`activo`** (soft-delete) ≠ **`estado`** (ciclo operativo). No mezclarlos.
- **`updated_at`** mantenido por trigger `set_updated_at()` (compartido).
- Relación propietario↔propiedad será N:M desde el inicio (copropiedad es realidad del dominio CL).

### Estrategias de Performance
<!-- pendiente -->

## Stored Procedures
- Helper SQL `auth_empresa_id()` y `auth_rol()` (Postgres functions) para usar en políticas RLS.

## APIs y Endpoints
- No se exponen endpoints REST propios de negocio. El acceso a datos es vía SDK Supabase con RLS. RPC/Edge Functions cuando se requiera lógica privilegiada.
- `POST /auth/signout` — route handler que cierra sesión y redirige a `/login`.
- Login: Server Action `signIn` (email/password) en `src/app/(auth)/login/actions.ts`.

## Autenticación y Acceso
- **Método:** email + password. **Sin auto-registro**: el admin da de alta a los usuarios (admins vía `bootstrap_admin.sql` manual; propietarios/arrendatarios vía "Invitar al portal", ver abajo). Decisión 2026-06-27.
- Páginas: `/login` (pública, redirige según `rol` si ya hay sesión); área privada admin bajo route group `(dashboard)`; área privada portal bajo route group `(portal)` (`/portal/...`).
- **Doble protección + gate de rol (T5, 2026-07-30):** middleware (redirige rutas privadas sin sesión) + cada layout exige `profile` y además redirige por rol — `DashboardLayout` manda a `/portal` si `rol !== 'admin'`; `PortalLayout` manda a `/dashboard` si `rol === 'admin'`. Login y `/login` (con sesión previa) redirigen a `/dashboard` o `/portal` según `profile.rol`.
- Raíz `/` redirige según sesión y rol.
- **Portal propietario/arrendatario (T20, 2026-07-30):** alta 100% manual desde la ficha admin ("Invitar al portal" → `invitarAlPortal` en `src/features/portal/actions.ts`). Usa `src/lib/supabase/admin.ts` (cliente `service_role`) — **solo se importa desde `features/portal/actions.ts`**, nunca desde un componente cliente ni ruta sin el gate `rol==='admin'` ya aplicado. Flujo: `generateLink` (tipo `invite` en la primera invitación, tipo `recovery` para reenviar a alguien ya invitado-no-confirmado, evita el error "email ya registrado") → upsert en `profiles` → vincula `profile_id` en `propietarios`/`arrendatarios` (`estado_invitacion: invitado`). El link generado se envía por **WhatsApp** (botón `wa.me` con mensaje precargado, `src/lib/whatsapp.ts`) — decisión explícita de no depender de email. `/auth/confirm` (route handler) resuelve `token_hash`+`type` de cualquier link de Supabase Auth (invite/recovery) y establece la sesión antes de mandar a `/portal/set-password`, donde el usuario define su contraseña. Al primer ingreso al portal, `PortalLayout` marca `estado_invitacion: activo`.
- **Gate de contraseña obligatorio (2026-07-31, migración `0031`):** `PortalLayout` redirige a `/portal/set-password` si `profile.password_set !== true`, ANTES de cualquier otra pantalla del portal (incluido `completar-perfil`) — corrige un bug real: alguien podía entrar con el link de invitación, ver su panel, y nunca definir contraseña, quedando sin forma de volver a entrar desde otro dispositivo. `/portal/set-password` muestra el email registrado ("Tu usuario para entrar será: ...") y llama a `marcarPasswordEstablecida()` (`src/features/portal/actions.ts`, cliente admin, marca su PROPIO profile) justo después de `supabase.auth.updateUser({ password })`. **Restablecer contraseña para cuentas ya activas**: `invitarAlPortal` ya no bloquea cuando `estado_invitacion === 'activo'` — genera un link tipo `recovery` igual que el reenvío a un `invitado`; `InvitarPortal` muestra el botón "Restablecer contraseña" en vez de "Enviar por WhatsApp" cuando ya está activo. Página **`/usuarios`** (admin, nueva) lista todos los propietarios/arrendatarios con algún estado de invitación y permite reenviar/restablecer desde un solo lugar sin ir ficha por ficha.
- **Bug corregido (2026-07-30, mismo día):** el link generado apuntaba a `properties.action_link` (endpoint hosteado de Supabase `/auth/v1/verify`), que al verificar entrega la sesión en un formato (fragmento de URL / `code`) que nuestra ruta `/auth/confirm` — basada en `token_hash`+`type`, el patrón oficial para entrega propia del link — no podía leer; el link se veía "caído" aunque el token fuera válido. Corregido construyendo el link nosotros mismos desde `properties.hashed_token` (`construirLinkConfirmacion` en `src/features/portal/actions.ts`). **Lección para cualquier flujo futuro que comparta un link de Supabase Auth por un canal propio (no el email de Supabase): siempre usar `hashed_token`, nunca `action_link`.**
- **Alta express sin ficha previa (2026-07-30):** además de invitar desde una ficha ya creada, `invitarNuevo` (mismo archivo) permite invitar solo con email + teléfono, sin crear antes la fila en `propietarios`/`arrendatarios` — pensado para que el admin no tenga que llenar los datos de la persona. Páginas `/arrendatarios/invitar` y `/propietarios/invitar` (botón "Invitar por WhatsApp" en el listado, junto a "Nuevo..."; **no reemplaza** la creación manual completa, es una vía adicional). La persona invitada completa su propia ficha (mismo wizard que usa el admin — `ArrendatarioWizard`/`PropietarioWizard`, ahora parametrizables vía `cancelarHref`/`draftKey`) en `/portal/completar-perfil`, gateado en `(portal)/layout.tsx`: si el profile del rol no tiene fila vinculada (`profile_id`) en `propietarios`/`arrendatarios`, se redirige ahí antes de ver cualquier otra pantalla del portal (detección de pathname vía header `x-pathname` seteado en `src/lib/supabase/middleware.ts`, ya que los Server Components no tienen acceso directo al pathname actual). El insert final (`completarPerfilArrendatario`/`completarPerfilPropietario`) usa el cliente admin — no existe política INSERT de estos roles sobre esas tablas por diseño; esta acción, gateada por rol + verificación de que aún no exista fila propia, es la única puerta de alta self-service.

## Observabilidad
<!-- pendiente: estrategia de logging/auditoría a definir (Supabase logs + tabla auditoría por empresa) -->

## Seguridad
- Autorización en BD vía RLS (no confiar en frontend).
- Secretos solo en variables de entorno (`.env.local`, Vercel env). `NEXT_PUBLIC_*` solo para claves anon públicas.
- `SUPABASE_SERVICE_ROLE_KEY` jamás se expone al cliente. **Uso hoy (2026-07-30):** `src/lib/supabase/admin.ts`, importado desde `src/features/portal/actions.ts` y `src/features/solicitudes-pago/actions.ts` — siempre detrás de un chequeo de rol (`getCurrentProfile()`) previo. No importar este cliente desde ningún otro archivo sin ese mismo gate.
- **Portal propietario/arrendatario:** RLS mayormente de solo lectura (sin INSERT/UPDATE/DELETE en la mayoría de las tablas para estos roles — ausencia de política deniega por defecto). Cada fila visible se resuelve por `profile_id = auth.uid()` en `propietarios`/`arrendatarios` y de ahí por las tablas puente ya existentes; nunca por email ni parámetro de request. **Única excepción (2026-07-30):** `solicitudes_pago` tiene INSERT real para `arrendatario` (política `solicitudes_pago_insert_arrendatario`, acotada por RLS a su propio contrato) — el resto de la escritura relacionada (crear el `pago` real al aprobar, que sí toca tablas admin-only) sigue mediada por el cliente admin en el server action, nunca por una política RLS de escritura sobre `pagos`/`documentos` para estos roles.

## Riesgos Activos
- [RIESGO] **Políticas RLS que se referencian cruzado entre dos tablas = recursión infinita, cae la app entera (no solo el rol afectado).** Incidente real 2026-07-30: la migración `0027` agregó una policy en `contratos_arrendatarios` que consultaba `contratos`; pero `contratos` ya tenía (desde `0024`) una policy que consulta `contratos_arrendatarios`. Postgres expande las políticas de AMBAS tablas para cualquier query que toque alguna de las dos — sin importar el rol de quien consulta — y al detectar el ciclo tira `infinite recursion detected in policy for relation "contratos"`, rompiendo `/portal`, `/contratos`, `/cobros`, el Dashboard, todo lo que tocara esas tablas, para todos los roles incluido admin. Vercel solo mostró "Application error" genérico con un `digest` — el mensaje real solo aparece en los Runtime Logs de Vercel. Corregido en `0028` con una función `SECURITY DEFINER` (mismo patrón que `auth_rol()`/`auth_empresa_id()`) para que la policy lea `contratos.propiedad_id` sin volver a disparar RLS sobre `contratos`. **Regla dura de aquí en adelante: ninguna policy RLS nueva puede hacer `select ... from <otra_tabla>` directo si esa otra tabla ya tiene (o podría tener) una policy que consulte de vuelta a la tabla original — usar una función `security definer` que bypasee RLS en esa lectura puntual.** Antes de agregar cualquier policy nueva, revisar qué políticas ya existen en las tablas que va a consultar.
- [RIESGO] RLS mal configurado = fuga de datos entre tenants. Mitigación: toda tabla nueva nace con RLS habilitado y políticas por `empresa_id`; revisión obligatoria.
- [RIESGO] Sincronización contrato↔propiedad NO atómica (dos escrituras separadas; Supabase-JS no hace transacción multi-statement desde el cliente). Severidad: Baja con concurrencia de admin. Mitigación: el contrato es la fuente de verdad y permite reconciliar. Si crece la concurrencia, mover a función Postgres con transacción.

## Problemas Conocidos
<!-- ninguno aún -->

## Deuda Técnica
- [PENDIENTE DE VERIFICAR] **Notificación push — flujo completo nunca probado end-to-end**: las claves VAPID ya están en Vercel y el sitio respondió bien tras el redeploy, pero nadie confirmó que una notificación real llegue a un dispositivo (Activar en Configuración/banner → informar un pago desde el portal → llega el aviso). Riesgo: Bajo (si falla, `notificarAdmins` no lanza y no rompe nada más — el arrendatario nunca ve un error por esto). Impacto: si no llega, el admin sigue sin saber que hay que ir a revisar `/cobros/solicitudes` a mano. Corrección: probar el flujo completo en un dispositivo real (idealmente Android, donde push funciona sin instalar la PWA) la próxima vez que se abra el proyecto.
- [DEUDA] **Estado de cuenta — link público sin autenticación del arrendatario**: `/e/[token]` muestra la deuda a quien tenga el link (token de ~190 bits, no adivinable, pero reenviable). Decisión consciente: el arrendatario debe poder abrirlo desde WhatsApp sin instalar ni loguearse. Riesgo: Bajo-Medio (expone monto de deuda, dirección y datos de contacto de esa persona si el link se filtra; no expone datos bancarios del propietario ni permite escribir nada). Impacto: un solo arrendatario por token. Corrección: cuando el portal esté liberado a clientes, ofrecer la variante autenticada (link al portal en vez de token público) y usar el token solo como fallback para quienes no tengan cuenta activa; mientras tanto, revocar el link tras confirmar el pago y/o setear `expira_en` al generarlo.
- [DEUDA] **Estado de cuenta — informe sin folio ni registro de envío**: no se guarda qué informe se envió, cuándo ni con qué saldo; el documento se recalcula en vivo cada vez que se abre el link. Riesgo: Bajo (para cobranza informal por WhatsApp basta). Impacto: no hay trazabilidad si el arrendatario discute qué se le informó y cuándo. Corrección: si la cobranza se formaliza (prejudicial, corte de servicios), agregar folio + snapshot del detalle al momento del envío y registrar el evento en auditoría.
- [DEUDA] **Solicitudes de pago — sin notificación proactiva**: cuando un arrendatario envía una solicitud, ni el propietario ni el admin reciben aviso (email/push/WhatsApp) — solo lo ven si entran a `/portal/solicitudes` o `/cobros/solicitudes` (este último con un banner de alerta si hay pendientes, pero solo se ve al entrar a Cobros). Riesgo: Medio si el propietario no revisa el portal seguido — un pago quedaría "reportado pero no confirmado" indefinidamente. Impacto: demora en confirmar pagos reales del arrendatario. Corrección: cuando exista algún canal de notificación (email transaccional, WhatsApp Business API, etc.), enganchar un aviso al crear la solicitud.
- [DEUDA] **Solicitudes de pago — comprobante rechazado queda huérfano en Storage**: si se rechaza una solicitud con comprobante adjunto, el archivo no se borra (solo se limpia si el usuario reintenta y sube uno nuevo... ni eso). Riesgo: Bajo (costo de storage marginal, sin fuga de datos — sigue aislado por `empresa_id` en el path). Impacto: acumulación lenta de archivos huérfanos. Corrección: job de limpieza periódico o borrado explícito al rechazar, si el volumen lo justifica.
- [DEUDA] **Portal — sin flujo de "revocar acceso"**: hoy solo existen los estados `sin_invitar → invitado → activo`; no hay acción de admin para desactivar el acceso de un propietario/arrendatario que ya está `activo` (ej. terminó el contrato, se vendió la propiedad). Riesgo: Bajo (el acceso sigue siendo de solo lectura y sigue acotado por RLS a sus propios contratos vigentes/pasados) — Impacto: un ex-arrendatario con cuenta activa mantiene acceso de lectura indefinido a su propio historial — Corrección: agregar acción "Desvincular del portal" (limpia `profile_id`, no borra el `auth.users` por si se reactiva) cuando se necesite.
- [DEUDA] **Portal — invitación depende de `NEXT_PUBLIC_SITE_URL` configurada en Vercel**: si la variable falta o queda desactualizada (cambio de dominio), el link generado por "Invitar al portal" apunta a un origen incorrecto. Riesgo: Bajo (se nota de inmediato al probar el link) — Impacto: invitaciones nuevas rotas hasta corregir la variable y redesplegar — Corrección: ninguna automática pendiente; si se agrega dominio propio, actualizar la variable en Vercel.
- [DEUDA] **Rol `corredor` / "propietario independiente" no implementado**: la discusión de ampliar el enum `rol_usuario` (mencionada en el cambio del 2026-07-26) quedó fuera de esta implementación de T20 a propósito — se limitó a los dos roles que ya existían en el enum (`propietario`, `arrendatario`). Riesgo: Bajo (no bloquea nada operativo hoy) — Impacto: si se necesita un perfil combinado propietario+corredor, requiere su propia migración de enum + políticas RLS — Corrección: nueva sesión de diseño cuando el negocio lo requiera.
- [DEUDA] **Cargos de luz/agua/internet creados antes del 2026-08-02 quedaron guardados con `tipo_cargo='otro'`**: la validación server-side (`src/features/cobros/actions.ts`) no incluía esos 3 valores hasta esta sesión — cualquier cargo de esos tipos ya insertado en producción antes del fix tiene el tipo mal etiquetado en la BD. Riesgo: Bajo (dato de clasificación, no afecta montos/saldos) — Impacto: reportes/filtros por tipo de cargo subestiman luz/agua/internet y sobrestiman "Otro" para esas filas históricas — Corrección: `update cargos set tipo_cargo = ... where ...` a mano, identificando por observaciones/monto/fecha cuáles corresponden a cada tipo real (no hay forma automática de saber cuál era la intención original).
- [DEUDA] **Recuperación de contraseña (`/recuperar-clave`) depende de que Supabase Auth tenga envío de correo configurado** (SMTP propio o el servicio limitado por defecto de Supabase) — a diferencia del resto del sistema, que evita el email a propósito y reparte todo por WhatsApp (links generados con `admin.generateLink`, nunca enviados por Supabase). Riesgo: Medio si nunca se configuró SMTP — el link de recuperación simplemente no le va a llegar a nadie, sin error visible para quien lo pide (mensaje genérico "revisa tu bandeja" se muestra igual, por diseño, para no filtrar qué emails existen). Impacto: usuarios que se autogestionan una clave olvidada quedan bloqueados sin aviso claro. Corrección: confirmar en el dashboard de Supabase (Authentication → Emails) si hay SMTP configurado; si no, decidir entre configurarlo o reemplazar este flujo por el mismo patrón manual-por-WhatsApp que ya usa el resto del sistema (admin genera el link de recovery y lo envía él mismo, como ya hace `invitarAlPortal` para "reenviar/restablecer").
- [RESUELTO 2026-08-02] **Submit implícito de formulario en wizards móviles no se resuelve interceptando la tecla Enter (`onKeyDown`)** — no es confiable entre teclados/navegadores (algunos disparan el submit sin pasar por un evento de teclado que JS pueda interceptar a tiempo). Bug real: el wizard de "Informar pago" guardaba la solicitud antes de terminar de subir el comprobante (caso real: solicitud de Paul Onetto, comprobante subido en el navegador pero nunca llegó a guardarse en la fila). Corregido exigiendo una marca explícita (`permitirSubmit`, puesta solo por el botón real de guardar o "Sí, guardar" al salir) en el `onSubmit` del `<form>` — cualquier submit espontáneo, sea cual sea su causa, queda bloqueado en el punto de origen. **Regla para cualquier wizard nuevo de varios pasos con un solo campo visible por pantalla: usar este patrón de marca explícita desde el principio, no confiar en interceptar teclas.**
- [RESUELTO 2026-07-30] **Links de un solo uso (magic link / invite) compartidos por WhatsApp: consumidos por el bot de preview antes de que la persona los toque.** WhatsApp (y otros mensajeros: Slack, Discord, Messenger) precargan cualquier URL compartida para generar la tarjeta de vista previa — un GET real al servidor. Si esa URL consume un token de un solo uso (`verifyOtp`/`token_hash` de Supabase Auth), el bot lo deja inválido y la persona ve "sesión inválida"/"inicia sesión" al tocar el link de verdad. Síntoma real: el link de invitación al portal, generado y enviado por WhatsApp, "no funcionaba" (pedía iniciar sesión) aunque el token fuera válido. Corregido con una página de aterrizaje pública (`/invitacion`, fuera de cualquier route group con gate de auth) que NO consume nada por sí sola — solo cuando la persona toca el botón "Registrarme" se navega recién ahí a `/auth/confirm` con el token real. **Regla para cualquier link de un solo uso que se comparta por un canal con preview automático (no solo WhatsApp): nunca poner el token en la URL que se comparte directamente — siempre interponer una página intermedia que requiera un click humano.**
- [RESUELTO 2026-07-29] **Convención de cálculo de fechas server-side**: no usar `new Date(...).toISOString()` para derivar mes/año en Node — la conversión a UTC puede retroceder un día (y el mes) en zona horaria con offset negativo (Chile). Bug real encontrado en `periodoArriendoVigente` (mostraba julio en vez de agosto); corregido con aritmética entera de meses. Aplica a cualquier cálculo futuro de "período siguiente/anterior" en el proyecto.
- [RESUELTO 2026-06-28] `codigo_interno` ahora se autogenera (`[2 iniciales comuna][inicial tipo][correlativo 4]`, ej. PRD0001) en la capa de app al crear, no editable. Correlativo por prefijo dentro de la empresa, con reintento ante colisión (unique constraint). Riesgo residual de carrera: Bajo (concurrencia de admin); si crece, mover a secuencia Postgres con bloqueo.
- [RESUELTO 2026-07-30] `database.types.ts` ya no se escribe a mano — se regenera con el CLI de Supabase (ver "Regenerar `database.types.ts`" en `## Restricciones de Cuenta / Hosting` para los comandos exactos, incluyendo el workaround de `npx.cmd`/`npm.cmd` por el bloqueo de execution policy de PowerShell). **Sigue pendiente automatizar**: tras cada `types:gen` hay que re-agregar a mano el bloque de alias de enums (`RolUsuario`, `CategoriaDocumento`, etc.) al final del archivo, porque la versión actual del CLI (2.110+) ya no los emite.
- [DEUDA] `comuna`/`region` como texto libre — Riesgo: Bajo — Impacto: inconsistencia de datos — Corrección: tablas catálogo `ref_regiones`/`ref_comunas` cuando se justifique reporting.
- [RESUELTO 2026-08-17] `contratos` no tenía columna/entidad de **garantía** — resuelto con tabla `contrato_garantias` (historial de movimientos: recepción/retención/devolución, migración `0042`), embebida como sección "Garantía" en `contratos/[id]`. El saldo disponible se calcula siempre en la query (nunca se guarda un total redundante). **Pendiente real**: los 2 contratos migrados de AssetPlan (803, 1907-A) siguen con la garantía documentada solo como texto libre en `observaciones` — falta cargar sus movimientos reales en la tabla nueva a mano (no se migró automáticamente).
- [DEUDA / FASE 2 — Gastos] **Obligaciones económicas normalizadas (responsabilidad compartida + cuotas)** — Riesgo: Medio — Impacto: modelo de gastos, cálculo de liquidaciones y reportes.
  - **Motivación (casos reales del negocio):**
    1. *Responsabilidad compartida*: hoy un gasto tiene un único `responsable_pago`. En la práctica un mismo gasto se reparte entre propietario y arrendatario (y a veces corredora) en porcentajes o montos distintos. Ej.: reparación $400.000 → propietario 75% / arrendatario 25%; mantención $120.000 → propietario $80.000 / arrendatario $40.000.
    2. *Pago en cuotas*: un gasto puede descontarse en varias liquidaciones/cobros. Ej.: reparación $600.000 en 6 cuotas de $100.000; comisión extraordinaria en 3 cuotas; seguro anual prorrateado mensual. Cada cuota con su propio estado (pendiente/pagada/anulada), fecha de vencimiento y asociación independiente a una liquidación o cobro.
  - **Diseño esperado (NO implementar ahora):** **no** extender la tabla `gastos` con más columnas. Normalizar con una **entidad hija** (`gasto_obligaciones` / `gasto_detalles` o equivalente) donde cada fila es una obligación económica independiente y soporta: responsable (propietario/arrendatario/corredora); porcentaje **o** monto fijo; una o múltiples cuotas; estados independientes por cuota; asociación individual a liquidación **o** cobro; y compatibilidad con los datos actuales (backfill: cada gasto existente = una obligación única). `gastos` quedaría como cabecera (descripción, categoría, propiedad, monto total, adjunto); las obligaciones/cuotas viven en la hija.
  - **Decisiones de la fase actual que dificultan esta evolución (flags explícitos):**
    - El **descuento automático** lee columnas directas de `gastos` (`responsable_pago='propietario'`, `descontar_de_liquidacion`, `liquidacion_id IS NULL`) en `liquidaciones/queries.calcularLiquidacion`, `gastosDescontables` y el reclamo/liberación en `liquidaciones/actions` (`generarLiquidacion`/`anularLiquidacion`). Al introducir la hija, ese cálculo debe **reapuntarse a las obligaciones/cuotas**, no al gasto. No rompe datos, pero es un refactor obligado de esa capa.
    - `gastos.liquidacion_id` es **un solo FK** por gasto: incompatible con cuotas asociadas a liquidaciones distintas. En Fase 2 la asociación debe vivir en la cuota; el `liquidacion_id` del gasto queda obsoleto (mantener para gastos de obligación única o migrar a la hija).
    - **Reportes** (`reportes/queries`) suman `gastos.monto` como total y consideran el gasto íntegro del propietario cuando `responsable_pago='propietario'`. Con responsabilidad compartida deben sumar **la porción de cada responsable** desde la hija (la rentabilidad del propietario solo debe descontar su parte). Refactor de la agregación de gastos.
    - Ninguno de estos puntos exige romper datos existentes: la hija se agrega de forma aditiva con backfill. Son acoplamientos de **código** a señalar, no de esquema.

<!-- Modelo de Contratos: IMPLEMENTADO en 0006. Se conserva el detalle aprobado abajo como referencia. -->
## Modelo aprobado — Contratos (IMPLEMENTADO en migración 0006)
Reglas de negocio confirmadas para cuando se construya:
- **1 contrato = 1 propiedad + N arrendatarios** → tabla puente `contratos_arrendatarios` (sin `arrendatario_id` directo en contrato). Soporta matrimonios, codeudores, representantes.
- **Canon vive en el contrato** (`canon_monto`, `canon_moneda` CLP/UF), independiente del `valor_referencial_arriendo` de la propiedad.
- **Reajuste**: enum `sin_reajuste | IPC | UF` + `periodicidad_reajuste_meses` (ej. 6, 12).
- **Comisión corredora**: `tipo_comision` (`porcentaje | monto_fijo`) + `comision_monto`.
- **Administración mensual**: `cobra_administracion` (bool) + `administracion_porcentaje` y/o `administracion_monto`.
- **Estados**: `borrador | vigente | vencido | terminado | renovado`.
- Campos mínimos: id, empresa_id, propiedad_id, numero_contrato, fecha_inicio, fecha_termino, canon_*, reajuste_*, comision_*, administracion_*, estado, observaciones, activo, timestamps.

## Roadmap
### Corto plazo
- Conectar Supabase real (migraciones 0001–0007 + bootstrap), validar el ciclo en runtime. Íconos PWA, push.
### Mediano plazo
- **Dashboard Operativo** (módulo estratégico, iniciado 2026-07-07): pantalla `/dashboard` con sección "Tareas pendientes" — liquidaciones y arriendos pendientes de generar, gastos pendientes de liquidar, comprobantes de pago sin adjuntar, contratos que vencen en 30 días. Diseño modular (`getTareasPendientes()` en `dashboard/queries.ts` devuelve un arreglo; agregar un indicador nuevo no requiere rediseñar la pantalla). Pendiente de evolución: **documentos sin respaldo obligatorio** (requiere definir qué documento es obligatorio por tipo de propiedad — regla de negocio no definida aún) y KPIs financieros más ricos (gráficos, tendencias) si se justifica.
- Liquidaciones a propietarios (canon − comisión − administración). Motor de reajuste automático (IPC/UF) según `periodicidad_reajuste_meses`.
- **Gastos Fase 2 — obligaciones económicas normalizadas** (responsabilidad compartida propietario/arrendatario/corredora por % o monto + pago en cuotas con estado y asociación independiente). Entidad hija de `gastos`; ver detalle y flags de acoplamiento en `## Deuda Técnica`.
### Largo plazo
- Documentos, tickets de mantención. Portal de propietario/arrendatario (políticas RLS específicas). Onboarding de segunda empresa (validar multitenancy).

## Roadmap de Hardening y Preparación para Producción
> Roadmap **oficial** derivado de las Investigaciones 1–5 (tributaria, seguridad/permisos/producción). Todas las tareas nacen en estado **⬜ Pendiente**; se ejecutarán sprint por sprint tras aprobación. Estados posibles: ⬜ Pendiente · 🔄 En curso · ✅ Hecho.
> Flags: **Migr** (migración) · **FE** (frontend) · **BE** (backend/Server Actions) · **RLS** · **Man** (prueba manual) · **Auto** (prueba automática). ✓ = requerido · – = no.

**Notas transversales:**
- No existe infraestructura de tests (Jest/Vitest y ESLint sin configurar). El framework se monta en **T16 (Sprint 2)**; hasta entonces el gate real es `tsc` + `next build` + prueba manual. Donde figura **Auto ✓** en Sprints 4–5 se asume T16 ya hecho.
- Migración de pulido **`0017_hardening`** = **T6** (RLS/índices). La identidad fiscal (**T9**) sale del hardening → migración propia posterior (p. ej. `0018_empresa_fiscal`).
- Dependencia raíz: **T1** (aplicar 0015/0016) → **T1b** (validación runtime) desbloquea T6, T11, T14.
- *(Ajuste 2026-07-03: T1 dividida en T1/T1b; T5 y T11 movidas a Sprint 1; T16 adelantada a Sprint 2; T9 reubicada al final del Sprint 3 como mejora independiente; nueva T22 de QA E2E como gate de producción.)*

### Sprint 1 — Crítico (pre-producción con datos reales)
| ID | Tarea | Prio | Estado | Deps | Migr | FE | BE | RLS | Man | Auto | Horas |
|----|-------|------|--------|------|:----:|:--:|:--:|:---:|:---:|:----:|------:|
| T1 | **Aplicar migraciones 0015 + 0016** en Supabase (SQL Editor). | Crítica | ✅ Hecho (2026-07-03) | — | ✓ aplicar | – | – | – | ✓ | – | 1–2 |
| T1b | **Validación runtime completa:** aislamiento entre tenants (archivo/gasto de empresa B inaccesible), bucket `public=false`, y QA 1–10 del flujo Gastos↔Liquidaciones. | Crítica | ✅ Aprobada c/obs. (2026-07-03) | T1 | – | – | – | ✓ verif. | ✓ | – | 3–5 |
| T2 | **Auditoría en operaciones financieras y contratos** (H1): `pago_registrado`/`pago_eliminado`, `cargo_creado`, contratos (creado/estado/terminado/vinculación) y catastro. | Crítica | ⬜ Pendiente | — | – | – | ✓ | – | ✓ | – | 4–6 |
| T3 | **Backups + prueba de restauración** (H3): PITR (upgrade) o `pg_dump` programado; restaurar en entorno de prueba. | Crítica | ⬜ Pendiente | — | – | – | – | – | ✓ | – | 3–5 |
| T5 | **Gate de rol en `DashboardLayout`** (H5, parte rápida): exigir `rol==='admin'` o redirigir. Elimina comportamiento extraño para futuros roles. | Alta | ✅ Hecho (2026-07-30, junto con T20) | — | – | ✓ | – | – | ✓ | – | 1 |
| T11 | **Regenerar `database.types.ts`** con `supabase gen types` (los tipos deben reflejar el esquema real antes de seguir desarrollando). | Alta | ✅ Hecho (2026-07-30) | T1 | – | – | ✓ | – | – | – | 1–2 |

*Riesgo si no se hace:* T1 → módulos Documentos/Gastos caídos. T1b → fuga entre empresas no detectada. T2 → sin traza de movimientos de dinero/contratos. T3 → pérdida irreversible de datos. T5 → ~~roles no-admin sin bloqueo limpio~~ resuelto. T11 → ~~tipos divergentes del esquema~~ resuelto. *Secuencia:* T1 → T1b → (T2 se valida dentro de T1b). **Pendiente real de Sprint 1: T2, T3.**

### Sprint 2 — Hardening
| ID | Tarea | Prio | Estado | Deps | Migr | FE | BE | RLS | Man | Auto | Horas |
|----|-------|------|--------|------|:----:|:--:|:--:|:---:|:---:|:----:|------:|
| T4 | **Logging server-side + fallos de auditoría** (H6+H7): capturar errores de Server Actions y loguear cuando `registrarAuditoria` falla. | Alta | ⬜ Pendiente | — | – | – | ✓ | – | ✓ | – | 3–4 |
| T6 | **Pulido de BD** (H8+H11): normalizar políticas antiguas a `to authenticated` + verificar índices en FKs de filtrado. *(migración `0017_hardening`)* | Media | ⬜ Pendiente | T1b | ✓ | – | – | ✓ | ✓ | – | 2–3 |
| T7 | **Revisión CSRF signout / rate-limit** (H9): confirmar protecciones de Supabase; opcional token anti-CSRF en `/auth/signout`. | Baja | ⬜ Pendiente | — | – | ✓ | ✓ | – | ✓ | – | 1–2 |
| T16 | **Infraestructura de tests** (Vitest/Jest) + ESLint + pruebas de reglas críticas (claim de gastos, cálculo de liquidación, RLS). *(adelantada desde Sprint 4 para reducir regresiones)* | Media | ⬜ Pendiente | — | – | – | ✓ | – | – | ✓ | 8–12 |

*Riesgo si no se hace:* T4 → operar a ciegas + huecos de auditoría. T6 → fragilidad/degradación. T7 → logout forzado (molestia). T16 → regresiones no detectadas al crecer. *Puede esperar meses:* T7. *Dependencia clave:* **T16 antes de T14/T15 (Sprint 4).** **Total ~14–21 h.**

### Sprint 3 — Preparación para producción
| ID | Tarea | Prio | Estado | Deps | Migr | FE | BE | RLS | Man | Auto | Horas |
|----|-------|------|--------|------|:----:|:--:|:--:|:---:|:---:|:----:|------:|
| T8 | **Monitoreo y alertas** (extiende H7): Sentry / alertas Vercel-Supabase (5xx, fallos de auth). | Media | ⬜ Pendiente | T4 | – | – | ✓ | – | ✓ | – | 3–4 |
| T10 | **Documentar convención "la fuente fiscal será el DTE"** en PROYECTO.md (no meter IVA en `gastos.monto`). Solo doc. | Media | ⬜ Pendiente | — | – | – | – | – | – | – | 0.5 |
| T12 | **Operativas menores:** reinstalar PWA ("RZK Prop"), renombrar `empresas.nombre`. | Baja | ⬜ Pendiente | — | – | – | – | – | ✓ | – | 0.5–1 |
| T13 | **Runbook de despliegue/rollback + health check** documentado. | Media | ⬜ Pendiente | — | – | – | – | – | ✓ | – | 2–3 |
| T22 | **QA funcional end-to-end (gate de producción):** flujo completo de negocio — crear propietario → propiedad → contrato → registrar cobro → registrar gasto → generar liquidación → anular → regenerar → subir documentos → verificar reportes. **Debe pasar antes de declarar el sistema listo para producción.** | Crítica | ⬜ Pendiente | T1b, T2, T5 | – | – | – | – | ✓ | – (automatizable tras T16) | 4–6 |
| T9 | **Identidad fiscal en `empresas`** (Inv4, mejora independiente post-hardening): RUT, giro, dirección tributaria (nullable); aparecen en PDFs. *(migración propia, p. ej. `0018_empresa_fiscal`)* | Media | ⬜ Pendiente | — | ✓ | ✓ | ✓ | – | ✓ | – | 2–3 |

*Riesgo si no se hace:* T8 → detección tardía de incidentes. T13 → despliegues sin reversa. **T22 → declarar producción sin validar el flujo completo (riesgo Alto).** T9 → sin base fiscal ni RUT en PDFs. *Nota:* T9 no es hardening; puede diferirse tras el go-live sin afectar la operación. *Puede esperar:* T12. **Total ~12–17.5 h.**

### Sprint 4 — Mejoras arquitectónicas (diferible meses)
| ID | Tarea | Prio | Estado | Deps | Migr | FE | BE | RLS | Man | Auto | Horas |
|----|-------|------|--------|------|:----:|:--:|:--:|:---:|:---:|:----:|------:|
| T14 | **Transaccionalidad vía RPC** (H4): funciones Postgres (`SECURITY INVOKER`) para creación de liquidación y sync `contrato↔propiedad`. | Media | ⬜ Pendiente | T1b, T16 | ✓ | – | ✓ | ✓ | ✓ | ✓ | 8–12 |
| T15 | **Auditoría por trigger DB-side** para eventos críticos (garantía independiente de la app). | Media | ⬜ Pendiente | T2, T16 | ✓ | – | ✓ | ✓ | ✓ | ✓ | 5–8 |

*Riesgo si no se hace:* T14 → estado parcial ante fallo (bajo con concurrencia de admin). T15 → auditoría dependiente de la app. *Requiere:* T16 ya hecho (Sprint 2). *Puede esperar meses.* **Total ~13–20 h.**

### Sprint 5 — Fase 2 (funcionalidades futuras, diferible muchos meses)
| ID | Tarea | Prio | Estado | Deps | Migr | FE | BE | RLS | Man | Auto | Horas |
|----|-------|------|--------|------|:----:|:--:|:--:|:---:|:---:|:----:|------:|
| T17 | **Gastos Fase 2** — entidad hija de obligaciones (responsabilidad compartida %/monto + cuotas con estado/vencimiento/asociación independiente). | Baja | ⬜ Pendiente | T14 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 20–30 |
| T18 | **Entidad Documentos Tributarios (DTE)** — tabla estructurada (dirección, folio, neto/IVA/exento, autorreferencia NC/ND) enlazada a documentos/gastos/pagos/liquidaciones. | Baja | ⬜ Pendiente | T9, T17 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 20–30 |
| T19 | **Reportes tributarios** (IVA débito/crédito, retenciones, insumos F29/RCV). | Baja | ⬜ Pendiente | T18 | – | ✓ | ✓ | – | ✓ | ✓ | 10–15 |
| T20 | **Portal propietario/arrendatario + RLS por rol** (completa H5). | Baja | ✅ Hecho (2026-07-30) | T2, T5 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 30–50 |
| T21 | **Integración SII** (certificado, CAF, XML, firma/timbre, certificación, envío) o vía PSTE; aislada en Edge Function. | Baja | ⬜ Pendiente | T18 | ✓ | – | ✓ | – | ✓ | ✓ | 40–80 |

*Riesgo si no se hace:* ninguno operativo hoy (single-tenant, uso interno) — son crecimiento, no estabilidad. *Dependencias para evitar retrabajo:* **T17 antes de T18** (modelar juntas: DTE↔gasto cabecera, cuotas↔liquidaciones); **T18 antes de T19/T21.** Si se terceriza facturación (PSTE), T21 colapsa a "almacenar el DTE resultante" (~10 h). *Puede esperar meses o más.* **Total ~120–205 h.**

*Nota T20:* se implementó **sin esperar T2** (auditoría completa aún ⬜ Pendiente) — decisión consciente para no bloquear el portal por una dependencia de Sprint 1 que sigue sin agendar. Solo queda un rastro liviano de la invitación (`invitado_por`/`invitado_en` en la fila de `propietarios`/`arrendatarios`), no una entrada en la tabla `auditoria`. Cuando T2 se implemente, evaluar si conviene además loguear ahí las invitaciones al portal.

### Resumen y reglas de ejecución
| Sprint | Foco | Prioridad | Esfuerzo | ¿Bloquea producción? |
|--------|------|-----------|---------:|----------------------|
| 1 | Aplicar migraciones + validación, auditoría de pagos, backups, ~~gate de rol~~, ~~tipos~~ | Crítica | ~13–21 h (T5+T11 ya hechos) | **Sí** (antes de datos reales) — falta T2, T3 |
| 2 | Logging, pulido BD, infra de tests | Alta/Media | ~14–21 h | Recomendado antes de escalar |
| 3 | Monitoreo, runbook, **QA E2E (gate)**, identidad fiscal | Media/Crítica | ~12–17.5 h | Recomendado (T22 obligatoria) |
| 4 | Transaccionalidad RPC, triggers DB-side | Media | ~13–20 h | No (diferible meses) |
| 5 | Gastos F2, DTE, reportes tributarios, ~~portal~~, SII | Baja | ~120–205 h (T20 ya hecho) | No (según crecimiento) |

- **Camino crítico mínimo para producción de un tenant:** Sprint 1 completo + T4 (logging) + **T22 (gate QA E2E)** → ~20–31 h.
- **Agrupaciones:** `0017_hardening` = T6 (solo); T9 en migración propia posterior; T1→T1b→T2 en la misma sesión de QA; T16 antes de T14/T15; T17+T18 modeladas juntas.
- **Siguiente paso al aprobar:** ejecutar **T1** (aplicar 0015/0016) → **T1b** (validación), que desbloquea T6, T11 y T14 y es el gate duro pendiente.

## Backlog QA 1
> Observaciones surgidas en la QA funcional de T1b (aprobada). Las **reglas de negocio** de esta sección son **oficiales y definitivas** — se usan como base de diseño, no se proponen alternativas. Pendientes de planificar/implementar (aún NO desarrolladas).

**Clasificación de los ítems:**

| Ítem | Tipo | Migración | Capa |
|------|------|:---------:|------|
| R1. Liquidaciones cerradas + "pendiente de liquidar" + arrastre a la siguiente | Regla de negocio | ✓ | BD + backend (motor de liquidación) |
| R2. Sin liquidaciones negativas → $0 + saldo del propietario con arrastre | Regla de negocio | ✓ | BD + backend |
| R3. Gastos (solo propietario/compartido) vs Cobros al Arrendatario; compartido genera cobro automático | Regla de negocio | ✓ | BD + backend + frontend |
| R4. Comprobante opcional al marcar gasto pagado | Regla de negocio | – (reusa `gastos.documento_id`) | frontend + lógica |
| R5. Gastos del propietario en cuotas (movimientos programados genéricos) | Regla de negocio | ✓ | BD + backend + frontend |
| M1. Editar participación de copropietarios | Mejora funcional | – | backend + frontend |
| M2. Mostrar nombre de propiedad en vez del ID | Mejora UX | – | frontend |
| M3. Contratos con etiqueta descriptiva (no solo número) | Mejora UX | – | frontend |
| M4. Separador de miles al escribir montos | Mejora UX | – | frontend |
| M5. Filtrar arrendatarios según la propiedad seleccionada | Mejora UX/funcional | – | frontend + query |
| M6. Estado "Anulado" en rojo | Mejora UX | – | frontend |
| M7. Renombrar label "Referencia" → "Observación" (pagos) | Mejora UX | – | frontend (solo label) |

### Reglas de negocio aprobadas (oficiales)
**R1 · Liquidaciones cerradas.** Una liquidación emitida NO se modifica ni recalcula automáticamente; **no existe reliquidación**. Un ingreso o gasto de un período ya liquidado queda marcado como **"pendiente de liquidar"**; la **siguiente** liquidación incorpora todos los movimientos pendientes hasta la fecha de corte. Debe haber trazabilidad de qué movimientos están liquidados y cuáles pendientes.

**R2 · Sin liquidaciones negativas.** Si el cálculo final < 0: el monto a transferir es **$0** y la diferencia queda como **saldo pendiente**, que se descuenta automáticamente de futuras liquidaciones hasta extinguirse, con trazabilidad completa. **El saldo es por (Propietario × Propiedad), NUNCA global.** Cada propiedad mantiene su propio historial y saldo; jamás se mezclan (ej.: Juan Pérez puede tener −$120.000 en Depto A y +$300.000 en Local Comercial, independientes).

**R3 · Gastos vs Cobros al Arrendatario.** Los **Gastos** representan solo lo que afecta al **propietario**. Los **Cobros al Arrendatario** representan cualquier monto que el arrendatario debe pagar.
- Gasto 100% propietario → solo Gasto; afecta liquidación; no genera cobro.
- Gasto **compartido** (solo **porcentaje**: 100/0, 80/20, 70/30, 50/50 — **sin montos fijos**) → **un único gasto**; el sistema descuenta la parte del propietario en la liquidación **y genera automáticamente un Cobro al Arrendatario** por su porcentaje. Sincronización del cobro: (a) se crea automáticamente al crear el gasto; (b) queda **vinculado** al gasto (`cargos.gasto_id`); (c) si el gasto cambia, el cobro **se actualiza**; (d) si el gasto se elimina y el cobro **no tiene pagos**, el cobro se elimina automáticamente; (e) si el cobro **ya tiene pagos**, se **impide** eliminar el gasto y se pide revertir el cobro primero.
- Gasto que sería 100% arrendatario → **no** se registra como Gasto; va directo como **Cobro al Arrendatario**.
- En Gastos se muestran solo **Propietario** y **Compartido**. **No se eliminan valores del enum** (`arrendatario`/`corredora` se conservan para compatibilidad histórica); simplemente **no se ofrecen en la UI**. Objetivo: eliminar duplicidad entre módulos.

**R4 · Comprobante.** Al marcar un gasto como pagado se solicita comprobante; **no es obligatorio**; se permite continuar sin adjuntarlo; se registra si existe o no.

**R5 · Gastos del propietario en cuotas.** Al crear un gasto: opción **Pago único** o **Pago en cuotas**. Si es en cuotas: indicar N° de cuotas, dividir el monto automáticamente, y generar **movimientos programados** vinculados al gasto original (ej. reparación $600.000 en 6 → 6 cuotas de $100.000). Cada liquidación descuenta **solo la cuota del período**; las cuotas futuras **NO** se transforman de inmediato en saldo pendiente. Si esa cuota provoca liquidación negativa, solo esa cuota genera saldo (R2). Trazabilidad completa gasto↔cuotas. **Diseño genérico**: el modelo de movimientos programados debe poder reutilizarse para seguros, mantenciones y pagos periódicos sin rediseñar.

### Plan de implementación QA1 (Fases A–H, aprobado 2026-07-03)
- **Fase A — Mejoras UX ✅ (2026-07-03, sin migración):** M6 Anulado en rojo · M2 propiedad con código+dirección · M3 contratos descriptivos · M4 separador de miles (`MoneyInput`) · M5 filtro de arrendatarios por propiedad · M7 label "Observación" · M1 editar participación de copropietarios. Build verde, `tsc` limpio. Commits locales `dbe2ea5`…`c390b00` (sin push).
- **Fase B — R4 ✅ (2026-07-03):** comprobante opcional al marcar gasto pagado (sube a Storage + registra `documento` categoría `comprobante_pago`, vincula `gastos.documento_id`; detalle indica si existe y permite verlo con signed URL). Sin migración. Build verde.
- **Fase C — R3** gastos compartidos + cobro automático sincronizado (migración: `+compartido` en UI, `porcentaje_propietario`, `cargos.gasto_id`).
- **Fase D — T16** infraestructura de tests (protege el motor).
- **Fase E — R1** liquidaciones cerradas + pendiente de liquidar (migración: `pagos.liquidacion_id` + backfill).
- **Fase F — R2** saldo por Propietario × Propiedad (migración: ledger de saldos).
- **Fase G — R5** gastos en cuotas / movimientos programados genéricos (migración).
- **QA completa del motor** tras F/G.
- **Fase H — UI de trazabilidad** (movimientos pendientes, cuotas y saldos) al final.
- *Dependencias:* C(R3)→E(R1)→F(R2)→G(R5) tocan `calcularLiquidacion` en ese orden; D (tests) antes de E/F/G.

## Simplificación de flujo — reglas oficiales (QA aprobada 2026-07-03)
> Reglas de negocio **oficiales y definitivas**. La app se centra en la **Propiedad**.

- **Flujo único:** Propiedad → Propietario (auto) → Contrato vigente (auto) → Arrendatario (auto). El usuario solo interviene si hay **>1 contrato vigente** para la propiedad.
- **Eliminar selectores manuales** de Propietario, Contrato, Arrendatario y **Responsable** cuando el dato se derive de la Propiedad. Sin decisiones duplicadas. Se conservan IDs/relaciones internas (compat histórica), pero **ocultos** en la UI.
- **Se elimina el concepto "Responsable" de la interfaz.**
  - **Gastos**: siempre del **propietario**. El usuario solo elige **Propiedad + datos del gasto**; nada de responsable/propietario/arrendatario/contrato.
  - **Cobros**: siempre del **arrendatario**. El usuario solo elige **Propiedad + datos del cobro**; el sistema deriva contrato vigente + arrendatario.
- **Gastos compartidos (futuro):** esta simplificación no lo bloquea; cuando llegue, se agrega el split por **porcentaje** propietario/arrendatario (no un selector de "Responsable"). Hasta entonces, formularios lo más simples posible.
- Regla general: si un dato se deduce de la Propiedad → eliminar el selector; mostrar lo derivado **solo lectura** cuando aporte contexto; solo pedir intervención si hay más de una alternativa válida.

### Inventario de selectores manuales (punto 6)
| Pantalla / archivo | Selector | Veredicto |
|---|---|---|
| Gastos — form (`gasto-form.tsx`) | **Responsable** (`responsable_pago`) | **Eliminar ya** (gasto siempre propietario; setear `propietario` en backend) |
| Gastos — form | **Propietario** (`propietario_id`) | **Eliminar ya** (derivar de la propiedad; hidden) |
| Gastos — form | Arrendatario (info del `SelectorPropiedadContrato`) | **Ocultar en Gastos** (irrelevante hasta compartidos; usar variante propiedad-only) |
| Documentos — form (`upload-form.tsx`) | **Propietario** (`propietario_id`) | **Eliminar ya** (derivar de la propiedad; hidden) |
| Cobros — form (`cargo-form.tsx`) | Propiedad→contrato(auto)→arrendatario(info) | **OK** (ya alineado) |
| Propiedades — `asignar-propietario.tsx` | Propietario (+% copropiedad) | **Mantener** (fuente de la relación propiedad↔propietario; no derivable) |
| Contratos — `asignar-arrendatario.tsx` | Arrendatario | **Mantener** (fuente de la relación contrato↔arrendatario) |
| Contratos — form nuevo/editar | Propiedad | **Mantener** (un contrato se crea sobre una propiedad) |
| Liquidaciones — `nueva` | Propietario | **Mantener por ahora**; **cambia** con R2 (saldo por propietario×propiedad) — la liquidación es por propietario |
| Filtros de listado (reportes/documentos/liquidaciones) | Propietario/Arrendatario | **Mantener** (son búsqueda, no alta; ya corregidos en QA3) |

**Eliminar inmediatamente (próximo sprint):** ~~Responsable y Propietario en form de Gastos; Propietario en form de Documentos; Gastos usa selector propiedad-only~~ **HECHO 2026-07-06.**
**Cambian con módulos futuros:** Gastos (split % al implementar compartidos); Liquidaciones (flujo/saldo con R2).

**Implementado (2026-07-06):** Gastos → solo Propiedad + datos (sin Responsable/Propietario/Arrendatario; `responsable_pago='propietario'` fijado en backend; contrato derivado/preservado en hidden; arrendatario oculto vía `mostrarArrendatario={false}`). Documentos → sin selector de Propietario. Build verde, `tsc` limpio, 12 tests verdes. **Se mantienen** (fuente de relación / core): asignar copropietario en Propiedades, asignar arrendatario y elegir propiedad en Contratos, selector de propietario en Liquidaciones (por revisar con R2), y los filtros de listado.

## Modalidad: Administrador de Contratos de Arriendo
> Administración directa de arriendos por Eduardo (propietario), reemplazando la gestión vía corredora (AssetPlan). Contrato base propio redactado (`docs/CONTRATO_BASE_ARRIENDO.md`, v2). Detalle completo del análisis, decisiones y transición de los 2 contratos existentes (803, 1907-A) en **`docs/ADMINISTRADOR_CONTRATOS_ARRIENDO_SESION_2026-07-24.md`** — incluye pendientes sin cerrar (firma de finiquitos, confirmación de razón social AssetPlan, papel de garantía de la encimera en 803, recepción de $895.000 de garantía traspasada). **Revisar ese documento antes de dar la transición por completamente cerrada o de ingresar estos 2 contratos como datos reales.**

## Últimos Cambios
- 2026-08-17 — **KPIs del Dashboard corregidos + 2 módulos nuevos: Garantía (sub-recurso de Contrato) e Incidencias de mantención.**
  - **KPI "Pagos recibidos"** (`features/dashboard/queries.ts`): no filtraba por `pago_directo_servicio`, así que sumaba también cargos de luz/agua/etc. que se pagan directo al proveedor (no a la corredora) — contradecía la regla de negocio ya definida en Estado de Cuenta (migración `0038`). Corregido excluyéndolos.
  - **Tarjeta "Deuda pendiente" → "Deuda vencida"**: el monto sumaba toda la deuda pendiente, incluida la que aún no vence; ahora solo suma cargos con `fecha_vencimiento < hoy` (mismo criterio ya usado en Cobros/Estado de Cuenta, antes solo aplicado al conteo de "Cargos morosos").
  - **Garantía** (migración `0042_contrato_garantias.sql`): tabla de movimientos (`recepcion`/`retencion`/`devolucion`) por contrato, sin columna de saldo — se calcula siempre en la query (`saldoGarantiaDisponible`). Embebida como sección propia en `contratos/[id]` (mismo patrón que "Arrendatarios del contrato": tabla + mini-formulario inline, sin wizard ni nav propios). Regla server-side: `retencion`/`devolucion` no pueden superar el saldo disponible recalculado en el servidor.
  - **Incidencias** (migración `0043_incidencias.sql`): módulo nuevo de nivel superior (`/incidencias`), siguiendo el patrón estándar de 9 módulos (tarjetas, edición en línea, wizard, filtro colapsable). Estados `reportada→agendada→en_proceso→resuelta/cancelada`; proveedor (nombre/contacto); vínculo opcional 1:1 a un Gasto (categoría "reparación") vía botón manual "Generar gasto", visible solo si `resuelta` + `costo` + sin `gasto_id` aún — no usa el mecanismo de "claim" atómico de Gastos↔Liquidaciones porque es una sola inserción por click, no un proceso batch. Tarea nueva en Dashboard: "Incidencias sin resolver".
  - Ambas tablas RLS solo-admin con `empresa_id` propio (sin join a `contratos`/`propiedades` en la policy) — mismo patrón que `gastos`/`documentos`/`cargos`, sin riesgo de la recursión RLS que ya tumbó producción dos veces (`0028`/`0034`).
  - **Migraciones `0042` y `0043` ejecutadas y confirmadas por Eduardo.** `database.types.ts` regenerado con el CLI real (`npx.cmd supabase gen types ...`) tras la confirmación — coincidió exacto con la edición manual hecha antes de correrlas; alias de enums re-agregados a mano al final, como en cada regen (el CLI no los emite).
  - `tsc --noEmit`, `next build` y Vitest (28 tests) verdes. **Ningún cambio se verificó en navegador real** (sin Playwright/chromium-cli en el entorno).
- 2026-08-15 — **Ronda de correcciones de UI + bug real de performance en el Dashboard (N+1 en liquidaciones pendientes).**
  - **Editar cargo y editar pago** (antes solo se podían crear): `actualizarCargo`/`editarPago` en `features/cobros/actions.ts`, edición en línea en `detalle-cargo.tsx` (mismo patrón que Propietarios/Contratos) y wizard de pago reutilizado con `pagoExistente`. Listado de pagos pasa de `<table>` a tarjeta estándar (`pagos-lista.tsx`).
  - **Wizard "Informar pago" del portal** guarda borrador en `localStorage` (solo al crear, no al editar) — antes se perdía todo si el arrendatario salía a WhatsApp y volvía.
  - **Bug real corregido — combos que no reabrían bien**: `Combobox`/`ComboboxOpcion`/`SelectCombo` (usados en ~35 selectores de toda la app: Propiedad, Región/Comuna, Banco, Tipo de cuenta, Estado, Tipo de cargo, Medio de pago, etc.) reabrían un valor ya elegido mostrando solo esa opción, porque el texto del campo (el label ya seleccionado) se usaba también como filtro contra sí mismo. Corregido con un flag "recién abierto" que muestra todas las opciones hasta que el usuario escribe. También dependían solo de `onBlur` para cerrarse (poco confiable en PWA/navegadores móviles) — se agregó un listener de clic/touch fuera del componente, y alternar abrir/cerrar al re-tocar el input ya enfocado.
  - **"Ver más información" → "Ocultar información"** al expandir, en las 11 pantallas que usan ese patrón (Tailwind `group-open`, sin JS).
  - Logo "RZK Prop" del sidebar (admin y portal) ahora es link a inicio.
  - Filtro "Estado" de Cobros pasa al mismo `ComboboxOpcion` que el resto de los filtros de esa pantalla (antes select nativo).
  - Cobros: separador entre cargos más grueso que el interno de "ver más información"; cargos sin deuda desvanecidos; resumen del período pasa de "Deuda pendiente" a "Deuda vencida" (o "Deuda pagada: $monto" si no hay nada vencido).
  - **N+1 en `listPendientesLiquidar`** (`features/liquidaciones/queries.ts`, usada por el Dashboard y por `/liquidaciones`): recorría los propietarios activos uno por uno con `await` secuencial (~6 consultas por propietario dentro de `calcularLiquidacion`). Corregido a `Promise.all`. **Real pero no era la causa del "Dashboard se queda pegado"** que Eduardo seguía reportando después de este fix — con datos reales había solo 1 propietario activo, insuficiente para explicar un colgado visible.
  - **Hipótesis descartada — `networkTimeoutSeconds` en `sw.ts`**: se probó agregar timeout a las reglas `NetworkFirst` de navegación del service worker (`defaultCache` de `@serwist/next/worker` no trae timeout, ver commit `8964782`), pensando que una conexión colgada explicaba el "Dashboard pegado". No resolvió el problema — Eduardo siguió reportando el mismo síntoma después. **Revertido** (commit `bdd88c0`).
  - **`loading.tsx` en `/dashboard` y `/portal` — agregado y luego revertido**: la intención era mostrar un esqueleto instantáneo en vez de pantalla congelada durante la navegación (commit `afa2b90`). Con logs reales de Vercel (`vercel logs`) se confirmó que el backend respondía normal (0.5–3.5s por carga, sin errores) pero aun así **Vercel registraba entre 7 y 12 invocaciones completas e independientes de `/dashboard` en menos de 30 segundos** — algo automático (no el usuario, confirmado con Eduardo; no la señal, pasaba igual en WiFi; no volumen de consultas, pasaba igual con una sola tarjeta) seguía re-pidiendo la página a un ritmo fijo de ~2-3s. **Causa de fondo sin identificar** — se sospechó ahorro de batería/Doze de Android (descartado por Eduardo, siempre estuvo así) y el mecanismo interno del service worker (`navigationPreload`), sin confirmar cuál. Eduardo confirmó que **antes de agregar `loading.tsx`, la navegación al Dashboard no se sentía lenta** — sin ese archivo, Next.js mantiene la pantalla anterior visible durante la navegación en vez de mostrar un estado nuevo, así que cualquier demora/reintento de fondo que ya existiera quedaba invisible. Al agregar `loading.tsx` esa condición preexistente se volvió visible como pantalla "pegada". **Revertido junto con todo lo demás de esta ronda** (commit `bdd88c0`) para devolver el comportamiento exacto de antes. **Pendiente real**: si se quiere reintroducir una pantalla de carga en el futuro, investigar primero por qué Vercel recibe múltiples invocaciones repetidas de una misma navegación — probablemente relacionado con el service worker (`navigationPreload`/`NetworkFirst`) o con el propio Chrome/WebView en el dispositivo, no con el código de la app (no hay `setInterval` ni refresco automático en ningún componente).
- 2026-08-07 — **Ronda de ajustes de portal + pagos directos a servicios (migración `0038`).**
  - **Pagos directos a servicios (`cargos.pago_directo_servicio`)**: se configura por cargo al crearlo, y el wizard solo pregunta en los tipos de servicio (`TIPOS_DESFAZADOS`) — en arriendo o multa el paso se omite porque siempre se transfieren. El estado de cuenta separa **"a transferir"** (que manda el total y las cuentas de destino) de **"servicios que pagas directo"**, que se listan como recordatorio con su propio subtotal y explícitamente fuera del total. Cobrar por transferencia algo que el arrendatario ya pagó en la cuenta del servicio sería un error de cobranza. El listado de admin muestra los tres montos separados (a cobrar / directo / por vencer).
  - Cargos del portal: nivel superior por **período colapsable** (abierto solo si queda deuda), orden por estado → vencimiento → item con arriendo primero, vencimiento en negrita, y los pagados resumidos en una línea plegable con "ver más".
  - **`terminoMostrar`** (`features/contratos/vigencia.ts`, con tests): un contrato sin fecha de término, o con fecha pasada pero aún vigente/renovado, se muestra como indefinido en vez de una fecha vieja que sugiere que ya no rige (renovación tácita). **`numeroContratoMostrar`** cae a un id corto: `numero_contrato` viene nulo en los contratos reales, así que el "—" era permanente.
  - **`GuardiaAtras`**: el botón atrás del sistema ya no cierra la PWA desde el inicio (entrada centinela en el historial). Montado solo en portal y dashboard — interceptarlo en todas las pantallas rompería los botones "Volver" que usan `router.back()`.
  - Botón de menú móvil movido a la izquierda del logo.
  - **`vitest.config.ts`** nuevo: Vitest no lee los `paths` del tsconfig, así que el alias `@/` hay que declararlo ahí. Sin esto no se puede testear ningún módulo que importe con `@/`.
  - **Verificado con datos, no requiere código**: los comprobantes de pago ya existen y son visibles para el arrendatario; lo que falta en Documentos son los **contratos**, que nadie ha subido (no hay ninguna fila con categoría `contrato`). Eduardo los subirá como archivo — no se generarán desde la plantilla.
  - **Pendiente de ejecutar**: `supabase/migrations/0038_cargo_pago_directo_servicio.sql` (el campo se agregó a mano a los tipos mientras tanto).
  - **Notificación push al admin (migración `0040`)**: cuando un arrendatario informa un pago, los admin de la empresa reciben una notificación del sistema. Cierra la deuda técnica "solicitudes de pago sin notificación proactiva". Piezas: tabla `push_suscripciones` (RLS solo sobre la propia fila, sin `exists` a otras tablas para no reabrir el ciclo de recursión de 0028/0035), handler `push`/`notificationclick` en `src/app/sw.ts`, `notificarAdmins` en `features/notificaciones/push.ts` y el control de activación por dispositivo en `/configuracion`. Dependencia nueva: `web-push`.
    - **`notificarAdmins` nunca lanza**: se llama después de que la solicitud ya está guardada, así que una falla de push no puede abortar la operación ni mostrarle un error al arrendatario por algo ajeno a él. Las suscripciones que el navegador invalida (404/410) se borran solas para no acumular endpoints muertos.
    - El estado de activación se lee del navegador, no de la base: el mismo admin puede tener notificaciones en el celular y no en el escritorio.
    - **Requiere variables de entorno en Vercel** (documentadas en `.env.example`): `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`. Sin ellas la app funciona normal y solo se omite el envío (queda un `console.warn`). En iPhone las push exigen que la PWA esté instalada en la pantalla de inicio.
    - **`BannerNotificaciones`** (`features/notificaciones/banner-notificaciones.tsx`), montado en el layout de `(dashboard)` — ya restringido a admin: ofrece activar sin que el admin tenga que saber que existe `/configuracion`. Reaparece en cada sesión nueva (decisión de Eduardo: la mayoría no va a buscarlo solo); si se cierra, no insiste de nuevo dentro de la misma sesión de pestaña (`sessionStorage`). No hay forma de que el navegador muestre el popup nativo de permiso sin un clic explícito — es una restricción de todos los navegadores contra el spam de permisos — así que el banner es lo más cerca posible: aparece solo, pero el paso final sigue pidiendo un toque. Lógica de suscripción compartida en el hook `usePush` (usado también por `ActivarNotificaciones` en Configuración).
  - **Comprobantes de pago mal ligados (migración `0039`)**: se creaban con `propiedad_id` null y todos con el mismo nombre, así que desaparecían del listado de Documentos al filtrar por propiedad/arrendatario y no había forma de saber a qué pago correspondía cada uno. `registrarDocumento` ahora deriva la propiedad desde el contrato (arreglo en la raíz, no en cada llamador) y ambos flujos guardan propiedad, arrendatario, fecha del pago y un nombre tipo "Comprobante de pago — Arriendo 08/2026". La `0039` corrigió las filas existentes. Nota: una solicitud **rechazada** no genera documento (su archivo queda huérfano en Storage, deuda ya registrada).
- 2026-08-03 (segunda ronda) — **Estado de cuenta enviable al arrendatario por WhatsApp (informe de deuda) — migración `0036`.** Módulo nuevo `src/features/estado-cuenta/`.
  - **Qué resuelve**: informe de cobranza con diseño de corredora para pedirle al arrendatario que confirme cuándo regulariza. **Solo cargos vencidos con saldo** (decisión de Eduardo, 2026-08-06, tras ver un informe real que incluía un cargo "Al día"): un cargo que aún no vence no se reclama, y uno pagado nunca aparece (`saldo_pendiente > 0` lo excluye; con abono parcial se informa el resto). El filtro va en la consulta (`fecha_vencimiento < hoy`), lo que además descarta los cargos sin fecha de vencimiento. El listado de admin queda alineado: solo lista arrendatarios en mora, pero muestra aparte el saldo por vencer como contexto interno.
  - **Dos vías de envío, por decisión explícita de Eduardo** (`wa.me` solo precarga texto, no permite adjuntar archivos): (1) **PDF** vía `window.print()` — mismo patrón que Liquidaciones; (2) **link público con token** (`/e/[token]`) que el arrendatario abre en el celular sin iniciar sesión. El botón de WhatsApp arma el mensaje con el total, la mora y el link si existe.
  - **Seguridad del link público**: token URL-safe de ~190 bits generado en la app, único vigente por arrendatario (generar uno nuevo revoca el anterior), revocable desde la UI y con soporte de expiración. La página pública **no usa RLS**: corre con `service_role` gateado por el token (`token-queries.ts`), y nunca recibe un `arrendatario_id` desde la URL — se deriva del token. **Decisión deliberada**: evita agregar policies `to anon` que cruzarían tablas y podrían reintroducir el ciclo de recursión de `0028`/`0035`. `/e/` se declaró pública en el middleware y el documento va con `robots: noindex`.
  - **Datos bancarios en BD, no hardcodeados**: `empresas` gana `banco`, `tipo_cuenta`, `numero_cuenta`, `rut_titular`, `titular_nombre`, `email_pagos`, editables en la nueva pantalla **`/configuracion`** (primera de su tipo; ítem nuevo en el sidebar).
  - **Destino del pago configurable por contrato (migración `0037`)**: `contratos.pago_directo_propietario` (default `false`). Por defecto el arrendatario transfiere a la corredora, que después liquida al propietario (flujo que la app ya implementa con Liquidaciones); marcado, el informe toma los datos bancarios **del propietario de esa propiedad** — campos que ya existían en `propietarios` y que hasta ahora solo se usaban para pagarle a él. Se decidió así tras que Eduardo cuestionara (con razón) si la cuenta correspondía al propietario: son dos flujos distintos de dinero (entrante vs. saliente) y cuál aplica depende del contrato. El checkbox está en el detalle del contrato, bloque de comisión/administración.
    - Un arrendatario puede deber cargos con destinos distintos: el informe agrupa por cuenta y muestra un bloque por destino con su subtotal (`DestinoPago` en `estado-cuenta/types.ts`). Con un solo destino se ve igual que antes, sin ruido.
    - **En copropiedad se usa el propietario de mayor `porcentaje_participacion`** — criterio determinista para no obligar a elegir una "cuenta de cobro" por propiedad. Si aparece un caso real donde no sirve, ahí conviene un campo explícito en `propietarios_propiedades`.
    - Si a una cuenta le faltan banco o número, ese bloque se omite del informe (no se muestra "Banco: —") y la vista admin avisa cuál falta, con link a Configuración o a la ficha del propietario según corresponda.
  - El documento (`documento.tsx`) es presentacional puro y lo comparten la vista admin y la pública — una sola maqueta para pantalla, impresión y arrendatario, sin riesgo de que se desincronicen. En móvil oculta columnas secundarias (período/atraso) y las reinyecta al imprimir.
  - Refactor DRY: `TIPO_CARGO_LABEL`/`etiquetaTipoCargo` centralizados en `features/cobros/constants.ts` — estaba duplicado en 3 archivos y este módulo era el cuarto consumidor.
  - Tests: `mora.ts` (cálculo de días de atraso) extraído como módulo puro y cubierto con 7 casos, incluidos cruces de mes/año y anclaje UTC — mismo tipo de bug de zona horaria que ya se corrigió en `periodoArriendoVigente`. Suite total: 20 tests verdes. `tsc` y `next build` limpios.
  - **PENDIENTE por decisión de Eduardo**: el informe **no** invita todavía a informar el pago por el portal, porque el portal aún no se libera a los clientes. Cuando se libere, agregar esa llamada a la acción en la sección "Cómo regularizar" (el flujo de informar pago ya existe y funciona).
  - **Estado de las migraciones**: `0036` ya aplicada y `database.types.ts` regenerado desde Supabase. **Pendiente de ejecutar: `0037_contrato_pago_directo_propietario.sql`** (el campo se agregó a mano a los tipos mientras tanto; regenerar después).
  - **Nota sobre `npm run types:gen`**: el script usa `--linked`, pero el proyecto no está vinculado; funciona con `npx supabase gen types typescript --project-id rrhhnfbxrxceedgueyss`. Además el `>` del script **trunca `database.types.ts` antes** de ejecutar el comando: si falla, deja el archivo vacío y no compila nada — generar primero a un temporal y recién ahí reemplazar. El generador tampoco emite los alias de enums del final del archivo (`TipoCargo`, `MedioPago`, etc.): hay que reponerlos a mano, como ya advierte el comentario de ese bloque.
- 2026-08-03 — **INCIDENTE: caída total del sistema por recursión RLS — migración `0035` (urgente).** La migración `0034` (RLS para que el arrendatario lea nombre/contacto del propietario) creó una referencia circular entre policies: `propietarios_propiedades_select_arrendatario` (0034) consultaba `contratos`, que ya tenía una policy (`contratos_select_propietario`, 0024) que consulta `propietarios_propiedades`/`propietarios` — Postgres detectó "infinite recursion detected in policy for relation contratos" y esto tumbó **toda la app** (contratos es tabla base de casi todos los módulos), no solo el portal. Es el mismo patrón exacto que ya rompió el sistema una vez antes (`0028`, con `contratos_arrendatarios`). Corregido con `0035`: dos funciones `SECURITY DEFINER` (`arrendatario_tiene_contrato_en_propiedad`, `arrendatario_ve_propietario`) que resuelven la relación arrendatario→propiedad→propietario bypasseando RLS, para que ninguna de las dos policies nuevas vuelva a tocar `contratos`/`propietarios_propiedades` con RLS activa. **Lección para toda policy RLS futura que cruce tablas**: antes de escribirla, revisar si alguna de las tablas involucradas ya tiene una policy que consulte de vuelta a la tabla donde se está agregando la nueva — si hay ciclo, usar función `SECURITY DEFINER` desde el inicio, no como parche posterior.
- 2026-08-02 (segunda ronda) — **Fix visual crítico + UX de cargos/pagos + detalle editable de pago informado + performance de /cobros.**
  - **Bug real**: el ícono de "ver contraseña" (login + portal) era invisible — `text-white/60` sobre un input con `bg-white` (icono blanco sobre fondo blanco). Corregido a `text-muted`/`hover:text-ink` en ambos formularios. Este era el motivo real por el que el punto de "ver contraseña" de la ronda anterior parecía no estar implementado (sí lo estaba, pero no se veía).
  - Ver comprobante (pago de cobro y comprobante de solicitud de pago) forzaba descarga automática (`createSignedUrl(..., { download: nombre })`) en vez de mostrarlo — quitado en ambos casos.
  - Header: "RZK" + "RZK Prop" redundante → "RZK" + "Prop" (sidebar admin y portal, desktop y drawer móvil).
  - Cargos: "Parcial" pasa de ámbar/celeste a gris (mismo tono que "Pendiente"); "Vencido" (rojo) ahora también se deriva en el portal (`/portal/cargos`), igual que ya se hacía en `/cobros` admin — antes dependía de una columna `estado='vencido'` que nunca se seteaba ahí.
  - Tarjetas de cobros/pagos (admin y portal): orden de campos a Período → Deuda pendiente → Monto (antes Monto/Saldo pendiente invertido, y "Deuda pendiente" como label suelto sin período).
  - **Nueva pantalla** `/portal/cargos/[id]`: reemplaza el botón "Editar pago informado" (que iba directo al wizard) por un ícono Ojo. El detalle muestra tarjetas de solo lectura (Cargo, Arrendatario, Propietario) + una sección "Pago informado" editable in-place (patrón `detalle-propiedad.tsx`: botón Editar → campos se vuelven input/select), reutilizando `editarSolicitudPago` (sigue bloqueado en backend si la solicitud ya fue revisada).
  - **Migración `0034` (pendiente de ejecutar en Supabase)**: nuevas policies RLS `propietarios_select_arrendatario` y `propietarios_propiedades_select_arrendatario` — el arrendatario ahora puede leer nombre/email/teléfono (no datos bancarios) del propietario de su propiedad, necesario para la pantalla de detalle nueva. **Eduardo debe correr `supabase/migrations/0034_propietarios_select_arrendatario.sql` en Supabase antes de que esa pantalla muestre los datos del propietario.**
  - Performance: volver de "Solicitudes de pago" a `/cobros` era lento porque `listCargos()` sin filtros traía el historial completo de cargos. Ahora, sin filtros explícitos, se acota a los **últimos 12 meses** más cualquier cargo con saldo pendiente (por antiguo que sea), con aviso visible y sin afectar el cálculo de deuda total. Filtros explícitos siguen mostrando lo que el usuario pida. ~~Versión inicial: "deuda viva + período actual"~~ — **demasiado estrecha, corregida el 2026-08-07**: escondía los cargos ya pagados de meses anteriores, así que un pago recién registrado desaparecía de la vista del admin (reportado con el agua de julio de Vicuña Mackenna: el arrendatario lo veía pagado y el admin no lo veía). Los datos siempre estuvieron correctos; era solo el filtro de presentación.
  - `tsc --noEmit` y `next build` verdes.
- 2026-08-02 — **Bugs reales encontrados probando en producción + edición de solicitud pendiente + autenticación + consistencia de UI.** Commits `94cc7e0`→`5ff11ed`→`520f4cd`→`3f71454`. Detalle completo en `## Punto de Continuación` (sesión 2026-08-02); resumen:
  - **Bug real**: cargos de luz/agua/internet se guardaban como `tipo_cargo='otro'` (faltaban en la lista de validación server-side) — corregido, pero los ya creados en producción quedan mal etiquetados y hay que corregirlos a mano.
  - **Bug real, 2 rondas**: submit implícito del wizard de pago/solicitud por teclado móvil guardaba antes de terminar de adjuntar el comprobante. Interceptar `onKeyDown` (primera ronda) no fue suficiente — caso real detectado: solicitud de Paul Onetto ($132.614) con comprobante subido pero nunca guardado en la fila. Fix definitivo: el `<form>` exige una marca explícita (`permitirSubmit`) puesta solo por la acción real de guardar antes de dejar pasar cualquier submit.
  - **Migración `0033`**: el arrendatario puede editar su solicitud de pago mientras siga `pendiente` (antes quedaba sin alternativa una vez informado el pago).
  - Autenticación: ver/ocultar contraseña (login + portal), nueva sección `/portal/cuenta` para cambiar clave, recuperación de clave desde login (`/recuperar-clave` → `/actualizar-clave`, depende de SMTP de Supabase Auth sin verificar), `/invitacion` salta directo al portal si ya hay sesión activa.
  - Tarjetas de Contratos: tipo de propiedad arriba (antes N° de contrato, a menudo vacío) + nombre completo de propiedad con numeración. `etiquetaPropiedad()` ya no antepone el código interno en ningún selector.
  - Dashboard: "Pagos generados" → "Pagos recibidos", medido por período del cargo (no `fecha_pago`).
  - ~40 selects nativos en 27 archivos → `SelectStyled` para consistencia visual.
  - `tsc`/`build`/Vitest (13 tests) verdes en cada entrega. **Falta**: re-probar el flujo de comprobante con el fix definitivo, y confirmar si Supabase tiene SMTP configurado para el flujo de recuperación de clave.
- 2026-07-31 (tercera ronda) — **Cargos "desfazados" (gasto_comun/luz/agua/internet) con período de consumo estructurado + aviso al terminar el contrato.** Eduardo consultó (exploratorio) cómo tratar boletas cuyo período de consumo no coincide con el mes de cobro/vencimiento (ej. luz 06/06–06/07, vence en julio); se acordó el criterio: `periodo` del cargo sigue siendo siempre el mes de **cobro/caja** (no el de consumo), consistente con cómo ya se organiza todo lo demás en el sistema. Implementado:
  - **Migración `0032`**: `cargos.fecha_consumo_desde`/`fecha_consumo_hasta` (fechas estructuradas, no texto libre en observaciones — decisión explícita para no depender de que se escriba bien cada vez). `TIPOS_DESFAZADOS` nuevo en `src/features/cobros/constants.ts` (gasto_comun, luz, agua, internet).
  - Wizard "Nuevo cargo" (`cargo-wizard.tsx`) gana 2 preguntas condicionales (desde/hasta) solo cuando el tipo es desfazado — primera vez que este wizard usa `omitirSi` (patrón ya usado en los wizards de Propietarios/Arrendatarios).
  - **Punto clave que Eduardo pidió no perder de vista**: al terminar un contrato, casi siempre falta generar el último cargo desfazado (el consumo real sigue corriendo hasta que el arrendatario se va) — relevante para no perder esa plata al devolver la garantía. Nueva tarea en el Dashboard + banner propio en Cobros ("contrato terminado con cargo desfazado sin generar"), calculado en `listContratosConDesfazadoPendiente()`. Se resuelve solo generando ese último cargo — sin botón de "marcar resuelto" (decisión explícita).
  - `tsc`/`build` verdes.
- 2026-07-31 (segunda ronda) — **Botones de wizard normalizados en los 5 que quedaban con el patrón viejo + regla del día 21 para la generación asistida.** (1) `propiedad-wizard.tsx`, `documento-wizard.tsx`, `gasto-wizard.tsx`, `cargo-wizard.tsx` y `seleccion-wizard.tsx` (Liquidaciones) todavía usaban `flex flex-wrap` para los 3 botones (Cancelar/Atrás caían juntos, Siguiente se iba a una fila aparte de ancho completo) — normalizados al `grid grid-cols-3` fijo que ya usaban el resto de los wizards (propietario, arrendatario, pago, solicitud de pago), con `<span/>` de relleno cuando no hay botón Atrás. (2) Nueva regla de negocio: el aviso "contratos sin arriendo generado" y el panel "Generación asistida" en Cobros/Dashboard solo se calculan a partir del **día 21** de cada mes (`debeAvisarGeneracionAsistida()` en `src/features/cobros/queries.ts`) — antes de esa fecha todavía hay tiempo de sobra para generar el arriendo del mes siguiente y el aviso solo generaba ruido.
- 2026-07-31 — **Monto de pago sin bloqueo (con marca de revisión) + gate de contraseña obligatorio + página de usuarios del portal.** Tres pedidos tras seguir probando el flujo de "Informar pago" y la invitación:
  - **Migración `0030`**: el monto informado por el arrendatario ya no se bloquea si supera el saldo pendiente del cargo (antes tiraba error) — se deja pasar con una advertencia visible en el wizard (no bloqueante), y queda marcado (`excede_saldo` + snapshot del saldo en `saldo_pendiente_al_crear`) para que el propietario/admin lo revise con más cuidado antes de aprobar (banner rojo + badge en `/portal/solicitudes` y `/cobros/solicitudes`).
  - **Migración `0031` — bug real de contraseña**: Eduardo invitó a un arrendatario, este completó el registro y vio su panel, pero nunca quedó con una contraseña utilizable desde otro dispositivo (nada forzaba pasar por `/portal/set-password`). Nueva columna `profiles.password_set` + gate duro en `PortalLayout` (redirige a set-password antes que cualquier otra pantalla, incluso antes de `completar-perfil`); `/portal/set-password` ahora muestra el email registrado y llama a `marcarPasswordEstablecida()` al terminar. De paso, "Restablecer contraseña" ya funciona también para cuentas `activo` (antes `invitarAlPortal` lo bloqueaba), y nueva página admin **`/usuarios`** para reenviar/restablecer sin ir ficha por ficha.
  - `tsc`/`build` verdes. **Falta**: prueba manual completa (Eduardo simulará el flujo de contraseña desde cero en un dispositivo nuevo).
- 2026-07-30 (incidente + cuarta ronda) — **App caída en producción por recursión RLS (ver `## Riesgos Activos` para el detalle completo) + fixes de UI en el portal del arrendatario.** Tras pushear la tercera ronda, Eduardo reportó "app caída" — `Application error: a server-side exception has occurred`. El log real de Vercel (`infinite recursion detected in policy for relation "contratos"`) identificó que la policy nueva de `0027` en `contratos_arrendatarios` consultaba `contratos`, que a su vez (desde `0024`) consultaba `contratos_arrendatarios` — ciclo que rompía ambas tablas para cualquier rol. Corregido en caliente con `0028_fix_recursion_contratos_arrendatarios.sql` (función `security definer` `propiedad_de_contrato()`, mismo patrón que `auth_rol()`). De paso, dos ajustes de UI reportados al probar el portal del arrendatario: (1) "Mis contratos" mostraba la dirección de la propiedad vacía — faltaba política RLS `propiedades_select_arrendatario` (nunca se agregó en `0024`, solo existía para propietario) — corregida en `0029_propiedades_select_arrendatario.sql`; (2) el botón "Solicitar pago" confundía (sonaba a pedir que le paguen) — renombrado a "Informar pago" en el botón, el wizard y los badges de estado ("Pago informado — esperando validación" / "Pago validado" / "Pago no validado"), con una nota explícita de que queda pendiente de validación del propietario. `tsc`/`build` verdes.
- 2026-07-30 (tercera ronda del día) — **Solicitudes de pago (arrendatario→propietario/admin) + paridad de info en portal + ajustes de Dashboard.** Nuevo lote de feedback tras probar la segunda ronda:
  - **Solicitudes de pago** (migraciones `0026_solicitudes_pago.sql` + `0027_contratos_arrendatarios_propietario.sql`, ejecutadas): el arrendatario "Solicita pago" desde `/portal/cargos` (wizard `SolicitudPagoWizard`, mismo set de preguntas que el admin), pero NO crea un `pago` real — queda `pendiente` hasta que el propietario (`/portal/solicitudes`) o el admin (`/cobros/solicitudes`, con banner de alerta en Cobros si hay pendientes) la aprueba (crea el `pago` real + recalcula el cargo + promueve el comprobante suelto a un `documento`) o la rechaza (con motivo, visible para el arrendatario). Es la única tabla con INSERT no-admin en todo el esquema (`solicitudes_pago_insert_arrendatario`, acotada a su propio contrato) — todo lo demás de escritura (crear el `pago`, tabla admin-only) sigue mediado por el cliente admin en el server action, nunca por RLS directa.
  - **Portal — paridad de información con la vista admin**: el detalle de contrato en el portal (`/portal/contratos/[id]`) era mucho más delgado que el de admin (faltaban moneda, comisión, administración, canon original, periodicidad, arrendatario) — ampliado a superset read-only de todos esos campos; el listado también suma el nombre del arrendatario en el disclosure. Requirió una policy RLS nueva (`0027`) porque el propietario no podía leer `contratos_arrendatarios` (solo el arrendatario tenía policy ahí).
  - **Dashboard**: KPI "Pagos generados" pasa de contar cantidad a mostrar el monto nominal en pesos (suma de pagos del mes), y deja de ser un link clicable (tarjeta informativa, sin flecha ni hover) — `Kpi` ahora acepta `href` opcional. "Deuda pendiente" gana el mismo ícono/color de alerta que "Cargos morosos" cuando hay saldo > 0.
  - Reutilización: `Kpi` (dashboard) ganó variante sin link; no se tocó nada del wizard admin de pago para esto.
  - `tsc --noEmit` y `next build` verdes. **Falta**: prueba manual completa del flujo de solicitud→aprobación (Eduardo va a simular ambos lados).
- 2026-07-30 — **Portal propietario/arrendatario (T20) + gate de rol (T5), sin commitear/pushear aún.** Origen: Eduardo preguntó cómo probar la vista de arrendatario con el email de su ficha; se investigó (research-only) el modelo de auth existente y se confirmó que no había vínculo real entre `arrendatarios.email`/`propietarios.email` y Supabase Auth, ni flujo de alta, ni RLS para esos roles — exactamente el T20 ya estimado en el roadmap (30–50 h, Sprint 5) y explícitamente diferido el 2026-07-26 por tocar RLS/seguridad. Se diseñó (plan aprobado por Eduardo: ambos roles en la misma pasada, invitación 100% manual del admin, link generado a copiar/enviar a mano sin depender de email) y se implementó completo en la misma sesión:
  - **Migración `0024_portal_invitaciones.sql`** (ejecutada por Eduardo en Supabase): columnas `profile_id`/`estado_invitacion`/`invitado_en`/`invitado_por` en `propietarios` y `arrendatarios`; políticas RLS de solo lectura nuevas en `propietarios`, `arrendatarios`, `propiedades`, `contratos`, `contratos_arrendatarios`, `cargos`, `pagos`, `liquidaciones`, `liquidacion_detalles`, `documentos`, `documento_versiones` y `storage.objects` — cada rol ve solo lo suyo vía `profile_id = auth.uid()` y las tablas puente ya existentes. Sin política de escritura para estos roles en ninguna tabla.
  - **`src/lib/supabase/admin.ts`** (cliente `service_role`, nuevo) + **`src/features/portal/actions.ts`** (`invitarAlPortal`): genera el link de invitación (`generateLink`, tipo `invite` la primera vez / `recovery` para reenviar a alguien `invitado` sin confirmar — evita el error de "email ya registrado"), crea/vincula el `profile` y la fila de `propietarios`/`arrendatarios`. El link se muestra en pantalla para copiar, **no se envía email** (decisión explícita de Eduardo).
  - **Gate de rol (T5)**: `(dashboard)/layout.tsx` redirige a `/portal` si `rol !== 'admin'`; nuevo `(portal)/layout.tsx` redirige a `/dashboard` si `rol === 'admin'`; login y `/login` redirigen según `rol`. `src/components/back-to-dashboard.tsx` generalizado con prop `home` para servir tanto a `/dashboard` como a `/portal`.
  - **Árbol `/portal/...`** (nuevo): inicio con KPIs simples según rol, mis contratos, mis cargos y pagos (arrendatario), mis propiedades y mis liquidaciones (propietario), mis documentos (ambos) — todo de solo lectura, mismo lenguaje visual burdeo/blanco que el resto de la app. `/portal/set-password` + `/auth/confirm` (route handler nuevo, patrón oficial de Supabase SSR para links de invite/recovery vía `token_hash`+`type`) resuelven el primer ingreso.
  - **Bloque "Invitar al portal"** nuevo en las fichas de Propietario y Arrendatario (`src/features/portal/invitar-portal.tsx`), con badge de estado y botón para invitar/reenviar.
  - **Contratiempos de entorno resueltos en la sesión**: `npm run types:gen` requiere `supabase login` interactivo (hecho por Eduardo) y el CLI vía `npx.cmd`/`npm.cmd` porque PowerShell bloqueaba los wrappers `.ps1` por política de ejecución; el archivo de tipos regenerado salió en UTF-16 por el redirect `>` de PowerShell (corregido a UTF-8); la nueva versión del CLI de Supabase ya no emite los alias de enum de nivel superior (`RolUsuario`, `CategoriaDocumento`, etc.) que el resto del código importa — se restauraron a mano al final de `database.types.ts` como `export type X = Database["public"]["Enums"]["x"]` (recordar re-agregar este bloque tras cada `types:gen` futuro).
  - `tsc --noEmit` y `next build` verdes. Commiteado y pusheado (`689d694`).
- 2026-07-30 (segunda mitad de la sesión, sobre el mismo Portal T20) — **Bug del link caído corregido + WhatsApp + comprobante de pagos + alta express sin ficha previa.** Eduardo probó el portal recién construido y reportó 4 pedidos en un solo mensaje:
  1. **Link de invitación "caído" — causa raíz encontrada y corregida**: `invitarAlPortal` usaba `properties.action_link` (apunta al endpoint hosteado `/auth/v1/verify` de Supabase, que entrega la sesión en un formato — fragmento de URL o `code` — que nuestra ruta `/auth/confirm`, basada en `token_hash`+`type`, no podía leer). Corregido construyendo el link desde `properties.hashed_token` (`construirLinkConfirmacion`). Se reemplazó además la UI de "copiar link" por un botón directo "Enviar por WhatsApp" (`src/lib/whatsapp.ts`, `wa.me` con mensaje precargado) en `InvitarPortal`, usando el teléfono ya registrado en la ficha.
  2. **Comprobante en pagos** (migración `0025_pagos_comprobante.sql`, **ejecutada por Eduardo tras un primer intento donde se regeneraron los tipos antes de correr el SQL** — quedó como lección de orden de pasos): `pagos.documento_id`, mismo patrón que `gastos.documento_id`. Se puede adjuntar durante el wizard "Registrar pago" (paso opcional al final, `src/features/cobros/pago-wizard.tsx`) o después desde la tabla de pagos en el detalle del cargo (`src/features/cobros/comprobante-pago.tsx`, nuevo). De paso se corrigieron los botones Cancelar/Atrás/Siguiente del wizard de pago al patrón `grid-cols-3` fijo (antes "Atrás" aparecía/desaparecía corriendo la fila).
  3. **KPI "Pagos generados" en el Dashboard** (conteo de pagos registrados en el mes en curso).
  4. **Alta express de propietario/arrendatario sin ficha previa** (`invitarNuevo` en `src/features/portal/actions.ts`): el admin solo ingresa email + teléfono desde `/arrendatarios/invitar` o `/propietarios/invitar` (botón "Invitar por WhatsApp" nuevo en `PageHeader`, **adicional** a "Nuevo arrendatario/propietario", no lo reemplaza) — se crea el `auth.users`+`profile` pero **sin** fila en `propietarios`/`arrendatarios` todavía. La propia persona la completa en `/portal/completar-perfil` la primera vez que entra (reutiliza `ArrendatarioWizard`/`PropietarioWizard`, ahora parametrizados con `cancelarHref`/`draftKey` para poder usarse tanto en el flujo admin como en el self-service). `(portal)/layout.tsx` detecta la ausencia de fila vinculada (`profile_id`) y redirige ahí antes de mostrar cualquier otra pantalla; la detección de a qué ruta se está navegando usa un header `x-pathname` nuevo seteado en `src/lib/supabase/middleware.ts` (los Server Components no tienen acceso directo al pathname). El insert final usa el cliente admin (`completarPerfilArrendatario`/`completarPerfilPropietario`) — no hay política INSERT de estos roles sobre esas tablas por diseño.
  - `tsc --noEmit` y `next build` verdes tras regenerar tipos (con la lección de orden: **correr el SQL antes de regenerar tipos**, no al revés). Sin verificación visual en navegador real todavía.
- 2026-07-29 (segunda mitad de la sesión, 4 commits: `f6629b0`→`6409c8b`→`8c8be69`→`dd2eec8`) — **Corrección de formato tras feedback real de Eduardo (capturas del celular) + regla de negocio "arriendo por adelantado" + fechas dd/mm/yyyy en toda la app.** Resumen completo con el detalle de cada commit en `## Punto de Continuación` (arriba) — incluye: paneles de Cobros/Liquidaciones/Reportes a burdeo (con recalibración de la paleta de `charts.tsx`); bug corregido en `aplicarReajusteUF` (adelantaba la fecha de revisión indebidamente); overlay de "Cancelar" unificado en los 8 wizards (se encontró que `propiedad-wizard.tsx` había quedado con el estilo viejo); `periodoArriendoVigente()` nuevo (el arriendo se paga por adelantado — período a generar es el mes siguiente, no el actual) **con un bug real de zona horaria** (`toISOString()` retrocedía el mes en offset negativo, corregido con aritmética entera); `src/lib/fecha.ts` nuevo aplicado a ~25 sitios con fechas ISO crudas; "Registrar pago" migrado de formulario inline a wizard con botón de ancho completo (`/cobros/[id]/pagos/nuevo`); filtros de Propiedad/Propietario/Arrendatario en 5 páginas migrados a `ComboboxOpcion`; listado de cargos de Cobros reestructurado en acordeón por período. Ninguno de estos cambios se verificó en navegador real (sin Playwright/chromium-cli en el entorno) — todo el ajuste vino de capturas de pantalla reales que Eduardo mandó.
- 2026-07-29 (3 commits pusheados: `ae74a4a`→`85c0f0a`→`fb5afbc`, + corrección de formato en `bb5c404`) — **Botón atrás genérico, reajuste de canon vía UF con revisión manual, y menú completo con formato replicado.**
  1. **`ae74a4a`** — Botón atrás del hardware generalizado: en vez de saltar siempre a `/dashboard`, sube un nivel en la jerarquía de la ruta (`/contratos/[id]/editar` → `/contratos/[id]` → `/contratos` → `/dashboard`), calculado genéricamente desde el pathname (sin casos especiales por módulo).
  2. **`85c0f0a`** — **Reajuste de canon con UF real (mindicador.cl), decisión siempre manual.** Se probó primero un cron trimestral automático; se descartó tras encontrar con datos reales (screenshot del historial de pagos AssetPlan de Vicuña Mackenna) que la fórmula correcta es `canon_uf_base × UF del corte trimestral (1° mar/jun/sep/dic)` — validada exacta contra 4 meses reales — pero que **cada contrato heredado tiene su propia cadencia real** (Vicuña resultó trimestral en la práctica pese al contrato decir "anual"; Curiñanca sí es anual real). Un cron único habría reajustado alguno antes de tiempo. Diseño final: `canon_actual`/`canon_uf_base`/`fecha_proximo_reajuste` nuevos en `contratos` (migraciones `0021`–`0023`, **ejecutadas**); botones "Aplicar reajuste (UF)" y "Postergar N meses" en el detalle del contrato (nunca automático — puede haber arreglo informal con el arrendatario); aviso no bloqueante en Cobros y nueva tarea pendiente en Dashboard cuando hay contratos con reajuste vencido de revisar. De paso: `generarArriendosDelMes` tenía un bug (cobraba `canon_monto` en vez de `canon_actual`) — corregido.
  3. **`fb5afbc`** — **Menú completo activado** (Cobros, Liquidaciones, Gastos, Documentos, Reportes ya no ocultos) + **patrón de diseño replicado** en los 5: filtro colapsable (y fix del bug de "Limpiar" como `Link` inmediato en Cobros/Gastos/Documentos), wizard de una pregunta a la vez en las pantallas de creación (Liquidaciones con una variante liviana de 2 preguntas que solo arma la URL, sin tocar la vista previa/confirmación existente). Feedback de Eduardo tras probarlo: faltaban las **tarjetas** de listado (se quedó en `<table>`), el **combobox con buscador y fondo blanco** (se usó `<select>` plano) y la posición del mensaje de **Cancelar**. Corregido en el mismo cierre de sesión (sin commit propio aún): tarjetas `ui.listCard` en los 4 listados; `ComboboxOpcion` nuevo (`src/components/combobox-opcion.tsx`, variante de `Combobox` para listas `{id,label}` como Propiedad/Propietario) en los pasos de selección de propiedad de los wizards; mensaje de Cancelar movido a overlay fijo arriba de la pantalla; `AccionesArchivo` con variante `oscuro` (se había vuelto invisible sobre las tarjetas burdeo nuevas); `docs/PAGINAS_NUEVAS.md` nuevo (checklist de 6 puntos para no repetir el mismo hueco en la próxima página). No se pudo validar visualmente en navegador (sin Playwright/chromium-cli en el entorno) — validado solo por `tsc`/`build`.
  4. También esta sesión: Dashboard simplificado a 3 KPI (Propiedades vs arrendadas, Deuda pendiente, Cargos morosos); selector de fecha con salto directo de año en Propiedades (`FechaInput`, reemplaza `<input type="date">` nativo); banner PWA "Hay una versión nueva — Actualizar" (`SerwistProvider` + `skipWaiting:false`); parseo de montos acepta coma como separador decimal en toda la app.
- 2026-07-27 (5 commits: `0bb6edb`→`4fd0ab1`→`b0d663b`→`84f6718`→`db46805`, todos pusheados) — **Contratos con el patrón de diseño completo + ronda de ajustes de UX sobre dispositivo real.** Detalle línea por línea en `## Punto de Continuación` (arriba) y en `docs/DISENO_PAGINAS.md`; resumen: (1) Ajustes de campos en Propietarios/Arrendatarios (máscara de RUT, banco/tipo de cuenta como combobox real, N° de cuenta numérico, validación de email con salto de línea antes del `@`, teléfono filtrado, botones del wizard en grid fijo, confirmación de cancelar como overlay). (2) **Contratos** replicado completo (tarjetas+filtros, detalle en línea, wizard, eliminar validado contra `cargos`) — nueva migración `0020_contratos_delete.sql` **pendiente de ejecutar**. (3) Bug real encontrado y corregido en los 3 wizards existentes: la última pregunta de cada uno nunca se guardaba por falta de `name` en el input activo. (4) Botón atrás del Android ahora siempre vuelve a `/dashboard` (decisión de producto). (5) Dashboard rediseñado con tema burdeo, 3 indicadores nuevos (Ocupación %, Propietarios activos, Arrendatarios activos) y tarjetas KPI en fila compacta. Todo validado con `tsc`/`build`/`vitest` en cada commit.
- 2026-07-26 (ronda 3, ~20 commits) — **Patrón de diseño de páginas cerrado y replicado en Propiedades, Arrendatarios y Propietarios.** Detalle completo en `docs/DISENO_PAGINAS.md` (fuente de verdad); resumen: (1) **Detalle con edición en línea**: panel burdeo único por registro, botón "Editar" que convierte cada campo del grid en su input real (combobox región/comuna, selects, numéricos con teclado numérico) sin abrir formulario aparte ni modal; "Volver" con `router.back()` para no perder filtros de la lista de origen. (2) **Wizard de creación** (una pregunta por pantalla): reemplaza los formularios tradicionales de creación en los 3 módulos; solo 2 botones de navegación (Atrás/Siguiente, sin "Omitir"); borrador persistente en `localStorage` (sobrevive salir de la app a medio llenar); preguntas condicionales (ej. tipo de persona natural/jurídica cambia qué preguntas son obligatorias); cabecera compacta para no competir con el teclado del celular; confirmación propia (no nativa) al cancelar. (3) **Eliminar registro**: se descubrió que `propiedades`/`arrendatarios`/`propietarios` nunca tuvieron política RLS de `DELETE` (a propósito, solo baja lógica) — un `DELETE` sin política no falla, simplemente afecta 0 filas en silencio; se agregaron las migraciones `0017`/`0018`/`0019` (**pendientes de ejecutar por Eduardo**) + validación previa por relaciones bloqueantes (contratos/gastos/liquidaciones según la tabla) antes de ofrecer el botón, y confirmación "Sí/No" propia en vez de `confirm()` nativo. (4) **Filtros**: colapsables detrás de un ícono, panel gris (no blanco), y "Limpiar" ahora solo resetea el formulario en pantalla — "Aplicar" es el único que ejecuta el filtro (antes "Limpiar" navegaba de inmediato, quedó documentado como pendiente corregir igual en Cobros cuando le toque su rediseño). Se eliminaron los formularios de creación tradicionales huérfanos (`propiedad-form.tsx`, `arrendatario-form.tsx`, `propietario-form.tsx`). Todo validado con `tsc`/`build` en cada commit; **pendiente validación visual final por Eduardo en el celular** y las 3 migraciones de DELETE. Sigue pendiente replicar el patrón en **Contratos** (más complejo por sus relaciones obligatorias — ver dudas ya resueltas en `docs/DISENO_PAGINAS.md`).
- 2026-07-26 (ronda 2, feedback visual sobre dispositivo real) — **Ajustes de theming y tarjetas tras revisión en el celular.** (1) Canvas corregido de negro (#1b1b22) a gris grafito real (#34343d) — el tono anterior se veía negro. (2) Topbar móvil (antes blanco) ahora usa el mismo canvas gris. (3) Splash/PWA: `manifest.webmanifest` (`background_color`/`theme_color`) y `viewport.themeColor` en `layout.tsx` pasan de blanco/negro a burdeo y grafito respectivamente. (4) `PageHeader`: el botón "Nueva X" se reemplaza por un ícono "+" circular junto al título (afecta todas las páginas que usan este componente, no solo Propiedades). (5) Tarjetas de listado (`ui.listCard`) pasan de blancas a **burdeo**; info secundaria (comuna/tipo/valor, email/teléfono, fechas/canon según la página) se oculta detrás de un disclosure nativo (`<details>`) con ícono de información — solo lo esencial (código/nombre + estado) queda siempre visible. Acciones "Detalle/Editar" y "Activar/Desactivar" pasan de texto a íconos (lápiz, toggle on/off). Validado con `tsc --noEmit` y `next build` (verdes); sigue pendiente la validación visual completa en todas las pantallas restantes (Cobros, Liquidaciones, Gastos, Reportes, Documentos no se tocaron en esta ronda, siguen con el diseño anterior de tabla/tarjeta blanca).
- 2026-07-26 — **Etapa inicial de uso real: UI simplificada + datos de carga preparados.** (1) **Theming**: canvas de la app pasa de blanco a gris grafito oscuro (mismo tono del sidebar, `--color-canvas`); nuevos tokens `--color-canvas-fg`/`--color-canvas-muted` para texto legible directo sobre el canvas oscuro (usados en `PageHeader` y el saludo del dashboard); login pasa a fondo burdeo. Las tarjetas (`ui.card`) siguen blancas — sin cambios de contraste ahí. Validado con `tsc --noEmit` y `next build` (ambos verdes); **falta validación visual en navegador** por Eduardo antes de dar por cerrado el detalle de contraste en pantallas no auditadas una por una. (2) **Dashboard**: los KPIs ahora son tarjetas clicables que llevan al listado correspondiente; se elimina la sección "Accesos rápidos" (redundante con eso). (3) **Listados** de Propiedades/Propietarios/Arrendatarios/Contratos: de tabla HTML a grid de tarjetas (`ui.cardGrid`/`ui.listCard`), porque la tabla recortaba información en pantallas angostas. (4) **Sidebar**: oculto temporalmente todo módulo fuera de Propiedades/Propietarios/Arrendatarios/Contratos (Cobros, Liquidaciones, Gastos, Reportes, Documentos) — el código de esos módulos sigue intacto, solo se saca del menú mientras se valida el flujo con datos reales. (5) **Scripts preparados** (no ejecutados, a correr por Eduardo): `supabase/maintenance/cargar_datos_reales_803_1907A.sql` — carga a Eduardo como propietario, las 2 propiedades (803, 1907-A), sus arrendatarios (Jimmy, Paul) y los contratos directos, más el rename de empresa a "RZK Prop"; requiere ajustar fechas de firma/inicio y contactos antes de ejecutar. **Pendiente sin resolver esta sesión:** el modelo de roles (agregar `corredor` al enum, perfil "propietario independiente" con permisos combinados propietario+corredor, RLS por rol para portal propietario/arrendatario) — esto ya estaba estimado en el roadmap como **T20 (Sprint 5, 30–50 h)**; no se implementó de forma apurada junto con los cambios de UI porque toca RLS/seguridad y merece su propia sesión de diseño. Ver conversación de la sesión para el detalle completo de la discusión de roles.
- 2026-07-07 — **QA aprobada (mejoras UX/consistencia) — implementación completa.** (1) **Gastos**: se quita el filtro Responsable del listado y las columnas Propietario/Arrendatario/Responsable del detalle (redundantes tras la simplificación de flujo); se agrega descripción explicando el impacto del gasto en la liquidación. (2) **Documentos**: fix de bug real — al quitar el selector de Propietario (sesión anterior) nunca se implementó la derivación automática (`upload-form.tsx` leía un campo inexistente); ahora `registrarDocumento` deriva el propietario desde `propietarios_propiedades` cuando la propiedad tiene un único dueño (con copropiedad queda `null`, sin bloquear la subida). (3) **Cargos**: se agregan filtros (propiedad, arrendatario, estado —incluye "vencido" derivado—, período, rango de vencimiento); propiedad/arrendatario se resuelven vía el contrato (no son columnas directas de `cargos`). (4) **Liquidaciones**: el wizard consulta si ya existe una liquidación (propietario+período, no anulada) antes de mostrar "Confirmar y generar" y muestra un aviso en su lugar; se fusionan los bloques "Descuentos" y "Gastos descontados" en uno solo con etiqueta de origen por línea, en la vista previa y en el detalle; nuevo indicador "Liquidaciones pendientes de generar" (período actual) en `/liquidaciones` con acceso directo (`listPendientesLiquidar`). (5) **Cobros**: nuevo indicador "Arriendos pendientes de generar" (contratos vigentes sin cargo de arriendo del mes) en `/cobros`, con el mes de "Generación asistida" prellenado (`listContratosSinArriendo`). (6) **Dashboard Operativo**: nueva sección "Tareas pendientes" en `/dashboard` reutilizando las queries de pendientes ya creadas + gastos pendientes de liquidar + comprobantes de pago sin adjuntar + contratos que vencen en 30 días; diseño modular (`getTareasPendientes()`) pensado para agregar indicadores futuros sin rediseñar la pantalla. **Sin migraciones** (todo capa frontend/query, reutilizando relaciones existentes). Build verde, `tsc` limpio, 12 tests en cada commit. **8 commits: `6c4c0b8` (Gastos) → `5d92be7` (filtros Cargos) → `4aadc68` (guardia duplicados) → `4a10605` (fusión bloques) → `f6403a7` (pendientes Liquidaciones) → `ae8c13e` (pendientes Cobros) → `4ca4409` (Dashboard Operativo) → `7df2ff0` (docs PROYECTO.md). Push autorizado y ejecutado a `origin/main` (`0ca2e5d..7df2ff0`).** QA6 (matriz de prueba de esta ronda) entregada como artifact: https://claude.ai/code/artifact/bf888b5a-c9bb-4e6d-a9ee-73416c5505da — **pendiente de ejecución por Eduardo**.
- 2026-07-06 — **Simplificación de formularios (ítems "eliminar ya" del inventario).** Gastos: se quitan los selectores de **Responsable, Propietario y Arrendatario**; el usuario solo elige **Propiedad + datos**; `responsable_pago` se fija en `propietario` en backend; el contrato se deriva/preserva oculto; casilla "descontar de liquidación" siempre disponible. Documentos: se quita el selector de **Propietario** (derivado de la propiedad). Componente `SelectorPropiedadContrato` gana `mostrarArrendatario`. Se conservan columnas/IDs (compat histórica). Sin migración. Build verde, `tsc` limpio, 12 tests. Commits locales, **sin push**.
- 2026-07-03 — **QA de simplificación de flujo APROBADA ✅.** Reglas oficiales registradas (app centrada en Propiedad; flujo Propiedad→Propietario→Contrato→Arrendatario automático; se elimina "Responsable" de la UI; Gastos=propietario, Cobros=arrendatario) + **inventario de selectores manuales** (ver sección "Simplificación de flujo"). Pendiente de aprobación: sprint para eliminar Responsable/Propietario en form de Gastos y Propietario en form de Documentos. Sin cambios de código aún.
- 2026-07-03 — **QA3 (simplificación de flujo + fix de filtros).** (1) Flujo unificado **Propiedad → Contrato (auto) → Arrendatario (info)**: componente `SelectorPropiedadContrato` en Gastos, Documentos y Cobros — el usuario solo elige propiedad; el contrato vigente se autoselecciona (único) o se pide (varios); el arrendatario es **solo lectura** derivado del contrato. Se elimina la selección manual de arrendatario. (2) Eliminado el **filtro por Contrato** en Documentos. (3) **Fix de filtros de Documentos** (bug: devolvían vacío): Propietario/Arrendatario ahora filtran por las **propiedades relacionadas** (vía `propietarios_propiedades` y `contratos_arrendatarios`), no por el FK directo (casi siempre null); fecha filtra sobre la **fecha efectiva** (documento o subida) en memoria. (4) Documentos: contrato nunca seleccionable/principal; se asocia el vigente en segundo plano. **Tests**: se incorpora **Vitest** (`npm test`) con pruebas de filtrado de fecha y etiquetas (12 pruebas verdes). Sin migración; `contrato_id` se conserva. Build verde, `tsc` limpio.
- 2026-07-03 — **QA2 (ajustes UX/consistencia) implementada.** (1) Etiqueta única de propiedad **Código · Calle Número · Depto/Unidad** (`src/lib/propiedad.ts`) aplicada a selectores y listados de gastos, documentos, contratos, cobros y reportes. (2) Selector de contrato con formato descriptivo (N° · propiedad) + **autoselección** del único contrato vigente en el form de documentos. (3) **Eliminado el selector de contrato en Gastos** — el gasto pertenece a la **Propiedad**; se conserva `contrato_id` (hidden, sin migración) para trazabilidad; detalle prioriza la propiedad. Regla Fase C: el cobro de gasto compartido derivará el contrato vigente de la propiedad (único→auto; ninguno→bloquear con motivo; varios→pedir selección). (4) Validación de copropietarios **≠100%** con aviso ("falta X%") + acción "Agregar copropietario"; 100% muestra badge verde. (5) Columna **Propiedad** en la tabla de Documentos. Sin migración. Build verde, `tsc` limpio.
- 2026-07-03 — **QA1 · Fase B (R4 comprobante opcional) completada (✅).** Al marcar un gasto como pagado se puede adjuntar comprobante (no obligatorio): se sube a Storage y se registra como `documento` (categoría `comprobante_pago`) vinculado por `gastos.documento_id`; el detalle indica si existe y permite verlo (signed URL). Reusa el módulo Documentos; sin migración. Build verde, `tsc` limpio. Siguiente: Fase C (R3, gastos compartidos + cobro automático).
- 2026-07-03 — **QA1 · Fase A (Mejoras UX) completada (✅).** M1–M7: Anulado en rojo; propiedad muestra código+dirección; contratos con etiqueta descriptiva; separador de miles en montos (`MoneyInput` reutilizable, envía número crudo); filtro de arrendatarios por propiedad en el form de gastos; label "Referencia"→"Observación" (sin tocar la columna); edición inline de participación de copropietarios (valida ≤100%). Sin migración. Build verde, `tsc` limpio. 5 commits locales, sin push. Siguiente: Fase B (R4, comprobante opcional al pagar).
- 2026-07-03 — **Sprint 1 · T1b aprobada con observaciones (✅).** QA funcional del ciclo Gastos↔Liquidaciones exitosa; validó el funcionamiento del módulo. Observaciones trasladadas a **`## Backlog QA 1`** (4 reglas de negocio oficiales R1–R4 + 7 mejoras M1–M7, clasificadas). Pendiente: análisis de impacto y plan de implementación (aprobación previa antes de programar).
- 2026-07-03 — **Sprint 1 · T1 completada (✅):** migraciones `0015_documentos.sql` y `0016_gastos.sql` aplicadas en Supabase por Eduardo. Verificación de esquema con 22 checks (enums, tablas, índices, unique, triggers, RLS, políticas, bucket privado, FKs) → **todo OK**. Sin intervención manual pendiente. Siguiente: **T1b** (validación runtime: aislamiento entre tenants + QA 1–10 Gastos↔Liquidaciones). Aún **no** se ejecutó QA funcional ni se regeneró `database.types.ts` (T11).
- 2026-07-03 — **Roadmap de Hardening reorganizado (aprobado con ajustes).** T1 dividida en T1 (aplicar 0015/0016) + T1b (validación runtime); T5 (gate de rol) y T11 (regenerar tipos) movidas a Sprint 1; T16 (infra de tests) adelantada a Sprint 2; T9 (identidad fiscal) reubicada al final del Sprint 3 como mejora independiente post-hardening; **nueva T22** (QA funcional end-to-end como gate de producción). Sin cambios de arquitectura ni alcance. Todas las tareas siguen en ⬜ Pendiente. Sin cambios de código.
- 2026-07-03 — **Roadmap oficial de Hardening y Preparación para Producción** agregado (T1–T21, 5 sprints), derivado de las Investigaciones 1–5; todas las tareas en estado ⬜ Pendiente. Pendiente de aprobación para iniciar ejecución sprint por sprint. Sin cambios de código.
- 2026-07-03 — **Ciclo Gastos↔Liquidaciones cerrado (descuento automático).** Al generar una liquidación, `calcularLiquidacion` incorpora los gastos pendientes descontables del propietario (condiciones: `propiedad` del dueño, `estado=pendiente`, `descontar_de_liquidacion=true`, `responsable_pago=propietario`, `liquidacion_id IS NULL`, `fecha` ≤ mes del período; además `propietario_id` null o = el liquidado, para copropiedad). Orden del cálculo: ingresos → comisiones/descuentos → **gastos** → total. Persistencia: **reclamo atómico** (`UPDATE gastos SET liquidacion_id, estado='pagado' WHERE liquidacion_id IS NULL …` con `returning`) que impide doble asociación bajo concurrencia; el total se **recalcula con los gastos efectivamente reclamados**. Cada gasto queda como detalle (`referencia_tipo='gasto'`). **Anulación**: libera los gastos (`liquidacion_id=null`, `estado='pendiente'`) para re-liquidar. Auditoría de asociación (`gasto_asociado_liquidacion`) y liberación (`gasto_liberado_anulacion`). UI: sección "Gastos descontados" en vista previa y detalle. **Reportes sin cambios** (leen la tabla `gastos`, no los detalles; `pendiente`/`pagado` cuentan igual). Sin migración. Build verde, `tsc` limpio.
- 2026-07-03 — **Módulo Gastos + Reportes Financieros.**
  - **Gastos** (migración `0016_gastos.sql`, **requiere aplicarla en Supabase**): enums `categoria_gasto`/`estado_gasto`/`responsable_gasto` y tabla `gastos`. `propiedad_id` obligatorio; `contrato_id`/`propietario_id`/`arrendatario_id`/`liquidacion_id`/`documento_id` opcionales. Campos: categoría, descripción, monto, fecha, estado (pendiente/pagado/anulado), `responsable_pago` (propietario/arrendatario/corredora), `descontar_de_liquidacion`, observaciones, `creado_por`/email, timestamps. RLS solo-admin por `empresa_id`. **Fuente oficial de gastos.** Regla: solo gastos con responsable propietario (o `descontar_de_liquidacion`) afectan la rentabilidad del dueño; los del arrendatario/corredora no. Modelo preparado para descuento automático futuro en liquidación (`descontar_de_liquidacion` + `liquidacion_id`; índice parcial de pendientes por descontar). UI: listado con filtros + total vigente, form crear/editar, detalle con acciones (pagar/anular/reactivar/eliminar). Guardas: no editar/eliminar un gasto ya ligado a liquidación. Auditoría en todas las operaciones.
  - **Reportes Financieros** (sin migración): dashboard en `/reportes` con filtros **año/propiedad/propietario** (Ejecutivo omitido: no existe en el modelo; Empresa omitido: RLS ya aísla el tenant). KPIs (9): ingresos por arriendo, comisiones cobradas/pendientes, gastos, liquidaciones emitidas/pagadas, cobros pendientes, mora, vacancia. Gráficos SVG propios (barras/líneas/pie/agrupadas, sin dependencias): ingresos mensual, comparativo mensual (ingresos/gastos/comisiones), comparativo anual (Y-1 vs Y), gastos por categoría. Tablas: rentabilidad por propietario (ponderada por % de participación, neta de comisiones y gastos del dueño) y gastos por propiedad. Exportación CSV (nativa) / Excel (HTML-table `.xls`) / PDF (impresión). **Cálculo sobre datos primarios** (pagos/cargos/contratos/gastos/propiedades), no sobre liquidaciones (salvo los reportes de liquidaciones), para evitar doble conteo. Build verde.
- 2026-07-03 — **Centro Documental** (migración `0015_documentos.sql`, **requiere aplicarla en Supabase**). Enum `categoria_documento` (contrato, anexo, inventario, acta_entrega, acta_recepcion, liquidacion, comprobante_pago, factura, boleta, gasto, mantencion, otro). Tablas `documentos` (metadatos + relaciones opcionales a propietario/arrendatario/propiedad/contrato + `version_actual`) y `documento_versiones` (cada archivo subido, `unique(documento_id, version)`). RLS solo-admin por `empresa_id`. Bucket privado `documentos` en Storage con políticas RLS de aislamiento por tenant (primera carpeta del path = `empresa_id::text`). **Subida directa cliente→Storage** (no vía Server Action) para evitar el límite de ~4.5 MB de body de las funciones serverless de Vercel; el Server Action solo registra metadatos y limpia el objeto huérfano si el insert falla. Ver/descargar vía **signed URLs** (60s) generadas server-side. UI: sección Documentos con listado (filtros categoría/propiedad/contrato/propietario/arrendatario/rango de fechas + buscador nombre/observaciones), formulario de subida, detalle con tabla de versiones (ver, descargar, subir nueva versión, eliminar versión, eliminar documento completo). Auditoría en cada acción. Build verde. Máx. 25 MB por archivo.
- 2026-06-30 — **Rebranding a "RZK Prop"** en todo lo visible (UI, metadata, manifest/PWA, docs, seed), preservando tablas/migraciones y URLs reales. QA de Liquidaciones aprobado. Limpieza de datos de prueba (cargos/pagos/liquidaciones/auditoría borrados, corretaje reseteado; catastro conservado).
- 2026-06-29 — **Liquidaciones — ajustes finales** (migraciones `0012`–`0014`): ajustes manuales (ingreso/descuento + observación, `referencia_tipo='manual'`) en la vista previa con recálculo en vivo; **congelamiento** (la liquidación guarda subtotales/total/líneas y nunca recalcula tras crearse); **corretaje** controlado por `contratos.corretaje_liquidado` (se cobra una sola vez, se marca al liquidar, se revierte al anular); **bloqueo de edición** (marcar pagada y anular solo desde `pendiente`); **numeración correlativa** `LIQ-AAAA-000001` única por empresa, visible en listado/detalle/PDF. **Requiere aplicar `0012`, `0013`, `0014`.**
- 2026-06-29 — **Módulo Liquidaciones a propietarios** (Fase 1A, migración `0011`): cálculo automático por propietario+período (ingresos = pagos efectivos ponderados por % de participación; descuentos = comisión administración mensual + corretaje en mes de inicio), asistente con vista previa, listado con filtros, detalle con detalle de líneas, registro de pago, anulación, PDF vía impresión, y **sistema de auditoría** (`auditoria` + helper). RLS solo-admin. **Requiere aplicar `0011` en Supabase.** Reglas de negocio del cálculo documentadas; ajustes manuales/mantenciones quedan como estructura preparada (no UI aún).
- 2026-06-29 — Borrador automático extendido a formularios de propiedad y contrato. **`numero_contrato` autogenerado** (correlativo por empresa) y oculto del formulario, igual patrón que `codigo_interno`. En detalle de propiedad, el botón "Asignar propietario" se muestra solo si no hay propietarios asignados.
- 2026-06-29 — Formularios de personas (propietario/arrendatario): región/comuna como combobox dependiente (orden región→comuna), teléfono numérico con `+`, **Nombres/Apellidos** ambos obligatorios (persona natural), dirección separada en **Calle + Número** (migración `0009` agrega `numero`). **Borrador automático** en localStorage (`use-form-draft`) que restaura lo escrito si la app se recarga (causa del "se borraba todo": redeploys frecuentes). Cargo: se quita Administración del formulario y se agregan **Luz/Agua/Internet** (migración `0010` agrega valores al enum `tipo_cargo`). **Requiere aplicar 0009 y 0010 en Supabase.**
- 2026-06-28 — Propiedades: código interno **oculto** del form; orden tipo→región→comuna→resto; **región y comuna obligatorias** como **combobox con buscador** (catálogo Chile en `src/data/chile.ts`, comuna dependiente de región). Componente `src/components/combobox.tsx`.
- 2026-06-28 — Íconos PWA reales (edificio burdeo, `scripts/gen-icons.mjs` con sharp). **Código interno autogenerado** y no editable; **propiedades guardables incompletas** (migración `0008`, `direccion` nullable; mínimo crear = comuna+tipo). Build verde. **Requiere aplicar `0008` en Supabase.**
- 2026-06-28 — **Rediseño UI** (sistema de diseño grafito + burdeo): tokens en `globals.css` (@theme), tipografía Inter, clases reutilizables en `src/components/ui.ts`, sidebar lateral responsivo (`src/components/sidebar.tsx`), login con panel de marca, dashboard con KPIs (`features/dashboard`), y restyle de todos los listados/formularios/detalles (PageHeader, tarjetas, badges de estado, lucide-react). Build verde. Pendiente: íconos PWA reales.
- 2026-06-28 — MVP validado end-to-end en Supabase real; desplegado en Vercel (`ruzosky-corredora.vercel.app`), instalable en celular. Repo en `github.com/Laleke/ruzosky-corredora` (identidad git local = Laleke, no la de trabajo).
- 2026-06-27 — Módulo **Cobros** (Paso 6, cierra MVP): migración `0007_cobros.sql` (`cargos` + `pagos`), enums tipo_cargo/estado_cargo/medio_pago. Generación asistida de arriendos del mes (idempotente), cargos manuales, pagos parciales con recálculo de saldo/estado, `vencido` derivado en lectura, deuda total. Build verde.
- 2026-06-27 — Módulo **Contratos** (Paso 5, núcleo): migración `0006_contratos.sql` (+ `contratos_arrendatarios`), enums reajuste/comisión/estado, CRUD, asignación N:M de arrendatarios, y **sincronización automática contrato→propiedad** con el contrato como fuente de verdad. Build verde.
- 2026-06-27 — Módulo **Arrendatarios** (Paso 4): migración `0005_arrendatarios.sql` (reutiliza enum `tipo_persona`, RLS solo-admin, baja lógica) + CRUD completo. Aprobado y registrado el modelo de Contratos (próximo paso). Build verde.
- 2026-06-27 — Módulos **Propiedades** (Paso 2) y **relación N:M** (Paso 3): migraciones `0003_propiedades.sql` y `0004_propietarios_propiedades.sql`. Propiedades con enums (tipo/estado/moneda), `publicada`, `fecha_adquisicion`, CRUD + baja lógica. Tabla puente como única fuente de verdad; asignación/desasignación de propietarios con validación de participación ≤ 100% y detalle de propiedad mostrando copropietarios. Build verde.
- 2026-06-27 — Módulo **Propietarios** (Paso 1): migración `0002_propietarios.sql` con RLS solo-admin, validación de RUT (dígito verificador), CRUD completo (listado + alta + edición + baja lógica) y navegación. Build verde. Subido `@supabase/ssr` a 0.12.0 (la 0.5.2 rompía la inferencia de tipos de insert/update con supabase-js 2.108).
- 2026-06-27 — Flujo de login real (SSR): login email/password, logout, área privada con layout protegido y dashboard base por rol. Tipos de BD `empresas`/`profiles` definidos a mano. Commit `cc46b04`. Pendiente: `npm install` + `npm run build` para validar compilación.
- 2026-06-27 — Definido bootstrap del primer admin: `supabase/bootstrap_admin.sql` (manual, vía service_role en SQL Editor).
- 2026-06-27 — Git inicializado con primer commit local (`da65bc7`), sin remoto. Registrada restricción de scope personal y de no-push automático.
- 2026-06-27 — Creación del scaffold base: Next.js App Router, PWA (Serwist), clientes Supabase `@supabase/ssr`, migración inicial de tenancy/auth con RLS. Sin módulos de negocio.
